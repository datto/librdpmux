/*
 * Generated by gdbus-codegen 2.46.2. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "connector.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio-unix-2.0/gio/gunixfdlist.h>
#endif

typedef struct
{
    GDBusArgInfo parent_struct;
    gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
    GDBusMethodInfo parent_struct;
    const gchar *signal_name;
    gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
    GDBusSignalInfo parent_struct;
    const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
    GDBusPropertyInfo parent_struct;
    const gchar *hyphen_name;
    gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
    GDBusInterfaceInfo parent_struct;
    const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
    const _ExtendedGDBusPropertyInfo *info;
    guint prop_id;
    GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
    g_value_unset (&data->orig_value);
    g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
    gboolean ret = FALSE;
    guint n;
    if (a == NULL && b == NULL)
    {
        ret = TRUE;
        goto out;
    }
    if (a == NULL || b == NULL)
        goto out;
    if (g_strv_length (a) != g_strv_length (b))
        goto out;
    for (n = 0; a[n] != NULL; n++)
        if (g_strcmp0 (a[n], b[n]) != 0)
            goto out;
    ret = TRUE;
    out:
    return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
    gboolean ret = FALSE;
    if (a == NULL && b == NULL)
    {
        ret = TRUE;
        goto out;
    }
    if (a == NULL || b == NULL)
        goto out;
    ret = g_variant_equal (a, b);
    out:
    return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
    gboolean ret = FALSE;
    g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
    switch (G_VALUE_TYPE (a))
    {
        case G_TYPE_BOOLEAN:
            ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
            break;
        case G_TYPE_UCHAR:
            ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
            break;
        case G_TYPE_INT:
            ret = (g_value_get_int (a) == g_value_get_int (b));
            break;
        case G_TYPE_UINT:
            ret = (g_value_get_uint (a) == g_value_get_uint (b));
            break;
        case G_TYPE_INT64:
            ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
            break;
        case G_TYPE_UINT64:
            ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
            break;
        case G_TYPE_DOUBLE:
        {
            /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
            gdouble da = g_value_get_double (a);
            gdouble db = g_value_get_double (b);
            ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
            break;
        case G_TYPE_STRING:
            ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
            break;
        case G_TYPE_VARIANT:
            ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
            break;
        default:
            if (G_VALUE_TYPE (a) == G_TYPE_STRV)
                ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
            else
                g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
            break;
    }
    return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.DBus.Properties
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:MuxOrgFreedesktopDBusProperties
 * @title: MuxOrgFreedesktopDBusProperties
 * @short_description: Generated C code for the org.freedesktop.DBus.Properties D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.DBus.Properties ---- */

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_method_info_get_IN_ARG_interface_name =
        {
                {
                        -1,
                        (gchar *) "interface_name",
                        (gchar *) "s",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_method_info_get_IN_ARG_property_name =
        {
                {
                        -1,
                        (gchar *) "property_name",
                        (gchar *) "s",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_freedesktop_dbus_properties_method_info_get_IN_ARG_pointers[] =
        {
                &_mux_org_freedesktop_dbus_properties_method_info_get_IN_ARG_interface_name,
                &_mux_org_freedesktop_dbus_properties_method_info_get_IN_ARG_property_name,
                NULL
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_value =
        {
                {
                        -1,
                        (gchar *) "value",
                        (gchar *) "v",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_pointers[] =
        {
                &_mux_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_value,
                NULL
        };

static const _ExtendedGDBusMethodInfo _mux_org_freedesktop_dbus_properties_method_info_get =
        {
                {
                        -1,
                        (gchar *) "Get",
                        (GDBusArgInfo **) &_mux_org_freedesktop_dbus_properties_method_info_get_IN_ARG_pointers,
                        (GDBusArgInfo **) &_mux_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_pointers,
                        NULL
                },
                "handle-get",
                FALSE
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_interface_name =
        {
                {
                        -1,
                        (gchar *) "interface_name",
                        (gchar *) "s",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_pointers[] =
        {
                &_mux_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_interface_name,
                NULL
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_properties =
        {
                {
                        -1,
                        (gchar *) "properties",
                        (gchar *) "a{sv}",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_pointers[] =
        {
                &_mux_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_properties,
                NULL
        };

static const _ExtendedGDBusMethodInfo _mux_org_freedesktop_dbus_properties_method_info_get_all =
        {
                {
                        -1,
                        (gchar *) "GetAll",
                        (GDBusArgInfo **) &_mux_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_pointers,
                        (GDBusArgInfo **) &_mux_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_pointers,
                        NULL
                },
                "handle-get-all",
                FALSE
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_method_info_set_IN_ARG_interface_name =
        {
                {
                        -1,
                        (gchar *) "interface_name",
                        (gchar *) "s",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_method_info_set_IN_ARG_property_name =
        {
                {
                        -1,
                        (gchar *) "property_name",
                        (gchar *) "s",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_method_info_set_IN_ARG_value =
        {
                {
                        -1,
                        (gchar *) "value",
                        (gchar *) "v",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_freedesktop_dbus_properties_method_info_set_IN_ARG_pointers[] =
        {
                &_mux_org_freedesktop_dbus_properties_method_info_set_IN_ARG_interface_name,
                &_mux_org_freedesktop_dbus_properties_method_info_set_IN_ARG_property_name,
                &_mux_org_freedesktop_dbus_properties_method_info_set_IN_ARG_value,
                NULL
        };

static const _ExtendedGDBusMethodInfo _mux_org_freedesktop_dbus_properties_method_info_set =
        {
                {
                        -1,
                        (gchar *) "Set",
                        (GDBusArgInfo **) &_mux_org_freedesktop_dbus_properties_method_info_set_IN_ARG_pointers,
                        NULL,
                        NULL
                },
                "handle-set",
                FALSE
        };

static const _ExtendedGDBusMethodInfo * const _mux_org_freedesktop_dbus_properties_method_info_pointers[] =
        {
                &_mux_org_freedesktop_dbus_properties_method_info_get,
                &_mux_org_freedesktop_dbus_properties_method_info_get_all,
                &_mux_org_freedesktop_dbus_properties_method_info_set,
                NULL
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_interface_name =
        {
                {
                        -1,
                        (gchar *) "interface_name",
                        (gchar *) "s",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_changed_properties =
        {
                {
                        -1,
                        (gchar *) "changed_properties",
                        (gchar *) "a{sv}",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_invalidated_properties =
        {
                {
                        -1,
                        (gchar *) "invalidated_properties",
                        (gchar *) "as",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_pointers[] =
        {
                &_mux_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_interface_name,
                &_mux_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_changed_properties,
                &_mux_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_invalidated_properties,
                NULL
        };

static const _ExtendedGDBusSignalInfo _mux_org_freedesktop_dbus_properties_signal_info_properties_changed =
        {
                {
                        -1,
                        (gchar *) "PropertiesChanged",
                        (GDBusArgInfo **) &_mux_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_pointers,
                        NULL
                },
                "properties-changed"
        };

static const _ExtendedGDBusSignalInfo * const _mux_org_freedesktop_dbus_properties_signal_info_pointers[] =
        {
                &_mux_org_freedesktop_dbus_properties_signal_info_properties_changed,
                NULL
        };

static const _ExtendedGDBusInterfaceInfo _mux_org_freedesktop_dbus_properties_interface_info =
        {
                {
                        -1,
                        (gchar *) "org.freedesktop.DBus.Properties",
                        (GDBusMethodInfo **) &_mux_org_freedesktop_dbus_properties_method_info_pointers,
                        (GDBusSignalInfo **) &_mux_org_freedesktop_dbus_properties_signal_info_pointers,
                        NULL,
                        NULL
                },
                "org-freedesktop-dbus-properties",
        };


/**
 * mux_org_freedesktop_dbus_properties_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
mux_org_freedesktop_dbus_properties_interface_info (void)
{
    return (GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_properties_interface_info.parent_struct;
}

/**
 * mux_org_freedesktop_dbus_properties_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #MuxOrgFreedesktopDBusProperties interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
mux_org_freedesktop_dbus_properties_override_properties (GObjectClass *klass, guint property_id_begin)
{
    return property_id_begin - 1;
}



/**
 * MuxOrgFreedesktopDBusProperties:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>.
 */

/**
 * MuxOrgFreedesktopDBusPropertiesIface:
 * @parent_iface: The parent interface.
 * @handle_get: Handler for the #MuxOrgFreedesktopDBusProperties::handle-get signal.
 * @handle_get_all: Handler for the #MuxOrgFreedesktopDBusProperties::handle-get-all signal.
 * @handle_set: Handler for the #MuxOrgFreedesktopDBusProperties::handle-set signal.
 * @properties_changed: Handler for the #MuxOrgFreedesktopDBusProperties::properties-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>.
 */

typedef MuxOrgFreedesktopDBusPropertiesIface MuxOrgFreedesktopDBusPropertiesInterface;
G_DEFINE_INTERFACE (MuxOrgFreedesktopDBusProperties, mux_org_freedesktop_dbus_properties, G_TYPE_OBJECT);

static void
mux_org_freedesktop_dbus_properties_default_init (MuxOrgFreedesktopDBusPropertiesIface *iface)
{
    /* GObject signals for incoming D-Bus method calls: */
    /**
     * MuxOrgFreedesktopDBusProperties::handle-get:
     * @object: A #MuxOrgFreedesktopDBusProperties.
     * @invocation: A #GDBusMethodInvocation.
     * @arg_interface_name: Argument passed by remote caller.
     * @arg_property_name: Argument passed by remote caller.
     *
     * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method.
     *
     * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mux_org_freedesktop_dbus_properties_complete_get() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
     *
     * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
     */
    g_signal_new ("handle-get",
                  G_TYPE_FROM_INTERFACE (iface),
                  G_SIGNAL_RUN_LAST,
                  G_STRUCT_OFFSET (MuxOrgFreedesktopDBusPropertiesIface, handle_get),
                  g_signal_accumulator_true_handled,
                  NULL,
                  g_cclosure_marshal_generic,
                  G_TYPE_BOOLEAN,
                  3,
                  G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

    /**
     * MuxOrgFreedesktopDBusProperties::handle-get-all:
     * @object: A #MuxOrgFreedesktopDBusProperties.
     * @invocation: A #GDBusMethodInvocation.
     * @arg_interface_name: Argument passed by remote caller.
     *
     * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method.
     *
     * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mux_org_freedesktop_dbus_properties_complete_get_all() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
     *
     * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
     */
    g_signal_new ("handle-get-all",
                  G_TYPE_FROM_INTERFACE (iface),
                  G_SIGNAL_RUN_LAST,
                  G_STRUCT_OFFSET (MuxOrgFreedesktopDBusPropertiesIface, handle_get_all),
                  g_signal_accumulator_true_handled,
                  NULL,
                  g_cclosure_marshal_generic,
                  G_TYPE_BOOLEAN,
                  2,
                  G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

    /**
     * MuxOrgFreedesktopDBusProperties::handle-set:
     * @object: A #MuxOrgFreedesktopDBusProperties.
     * @invocation: A #GDBusMethodInvocation.
     * @arg_interface_name: Argument passed by remote caller.
     * @arg_property_name: Argument passed by remote caller.
     * @arg_value: Argument passed by remote caller.
     *
     * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method.
     *
     * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mux_org_freedesktop_dbus_properties_complete_set() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
     *
     * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
     */
    g_signal_new ("handle-set",
                  G_TYPE_FROM_INTERFACE (iface),
                  G_SIGNAL_RUN_LAST,
                  G_STRUCT_OFFSET (MuxOrgFreedesktopDBusPropertiesIface, handle_set),
                  g_signal_accumulator_true_handled,
                  NULL,
                  g_cclosure_marshal_generic,
                  G_TYPE_BOOLEAN,
                  4,
                  G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_VARIANT);

    /* GObject signals for received D-Bus signals: */
    /**
     * MuxOrgFreedesktopDBusProperties::properties-changed:
     * @object: A #MuxOrgFreedesktopDBusProperties.
     * @arg_interface_name: Argument.
     * @arg_changed_properties: Argument.
     * @arg_invalidated_properties: Argument.
     *
     * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-DBus-Properties.PropertiesChanged">"PropertiesChanged"</link> is received.
     *
     * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
     */
    g_signal_new ("properties-changed",
                  G_TYPE_FROM_INTERFACE (iface),
                  G_SIGNAL_RUN_LAST,
                  G_STRUCT_OFFSET (MuxOrgFreedesktopDBusPropertiesIface, properties_changed),
                  NULL,
                  NULL,
                  g_cclosure_marshal_generic,
                  G_TYPE_NONE,
                  3, G_TYPE_STRING, G_TYPE_VARIANT, G_TYPE_STRV);

}

/**
 * mux_org_freedesktop_dbus_properties_emit_properties_changed:
 * @object: A #MuxOrgFreedesktopDBusProperties.
 * @arg_interface_name: Argument to pass with the signal.
 * @arg_changed_properties: Argument to pass with the signal.
 * @arg_invalidated_properties: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-DBus-Properties.PropertiesChanged">"PropertiesChanged"</link> D-Bus signal.
 */
void
        mux_org_freedesktop_dbus_properties_emit_properties_changed (
        MuxOrgFreedesktopDBusProperties *object,
        const gchar *arg_interface_name,
        GVariant *arg_changed_properties,
const gchar *const *arg_invalidated_properties)
{
g_signal_emit_by_name (object, "properties-changed", arg_interface_name, arg_changed_properties, arg_invalidated_properties);
}

/**
 * mux_org_freedesktop_dbus_properties_call_get:
 * @proxy: A #MuxOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @arg_property_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_properties_call_get_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_properties_call_get_sync() for the synchronous, blocking version of this method.
 */
void
        mux_org_freedesktop_dbus_properties_call_get (
        MuxOrgFreedesktopDBusProperties *proxy,
        const gchar *arg_interface_name,
const gchar *arg_property_name,
        GCancellable *cancellable,
GAsyncReadyCallback callback,
        gpointer user_data)
{
g_dbus_proxy_call (G_DBUS_PROXY (proxy),
"Get",
g_variant_new ("(ss)",
arg_interface_name,
arg_property_name),
G_DBUS_CALL_FLAGS_NONE,
-1,
cancellable,
callback,
user_data);
}

/**
 * mux_org_freedesktop_dbus_properties_call_get_finish:
 * @proxy: A #MuxOrgFreedesktopDBusPropertiesProxy.
 * @out_value: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_properties_call_get().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_properties_call_get().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_freedesktop_dbus_properties_call_get_finish (
        MuxOrgFreedesktopDBusProperties *proxy,
        GVariant **out_value,
        GAsyncResult *res,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "(@v)",
                   out_value);
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_properties_call_get_sync:
 * @proxy: A #MuxOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @arg_property_name: Argument to pass with the method invocation.
 * @out_value: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_properties_call_get() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
        mux_org_freedesktop_dbus_properties_call_get_sync (
        MuxOrgFreedesktopDBusProperties *proxy,
        const gchar *arg_interface_name,
const gchar *arg_property_name,
        GVariant **out_value,
GCancellable *cancellable,
        GError **error)
{
GVariant *_ret;
_ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
                               "Get",
                               g_variant_new ("(ss)",
                                              arg_interface_name,
                                              arg_property_name),
                               G_DBUS_CALL_FLAGS_NONE,
                               -1,
                               cancellable,
                               error);
if (_ret == NULL)
goto _out;
g_variant_get (_ret,
"(@v)",
out_value);
g_variant_unref (_ret);
_out:
return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_properties_call_get_all:
 * @proxy: A #MuxOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_properties_call_get_all_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_properties_call_get_all_sync() for the synchronous, blocking version of this method.
 */
void
        mux_org_freedesktop_dbus_properties_call_get_all (
        MuxOrgFreedesktopDBusProperties *proxy,
        const gchar *arg_interface_name,
        GCancellable *cancellable,
GAsyncReadyCallback callback,
        gpointer user_data)
{
g_dbus_proxy_call (G_DBUS_PROXY (proxy),
"GetAll",
g_variant_new ("(s)",
arg_interface_name),
G_DBUS_CALL_FLAGS_NONE,
-1,
cancellable,
callback,
user_data);
}

/**
 * mux_org_freedesktop_dbus_properties_call_get_all_finish:
 * @proxy: A #MuxOrgFreedesktopDBusPropertiesProxy.
 * @out_properties: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_properties_call_get_all().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_properties_call_get_all().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_freedesktop_dbus_properties_call_get_all_finish (
        MuxOrgFreedesktopDBusProperties *proxy,
        GVariant **out_properties,
        GAsyncResult *res,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "(@a{sv})",
                   out_properties);
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_properties_call_get_all_sync:
 * @proxy: A #MuxOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @out_properties: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_properties_call_get_all() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
        mux_org_freedesktop_dbus_properties_call_get_all_sync (
        MuxOrgFreedesktopDBusProperties *proxy,
        const gchar *arg_interface_name,
        GVariant **out_properties,
GCancellable *cancellable,
        GError **error)
{
GVariant *_ret;
_ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
                               "GetAll",
                               g_variant_new ("(s)",
                                              arg_interface_name),
                               G_DBUS_CALL_FLAGS_NONE,
                               -1,
                               cancellable,
                               error);
if (_ret == NULL)
goto _out;
g_variant_get (_ret,
"(@a{sv})",
out_properties);
g_variant_unref (_ret);
_out:
return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_properties_call_set:
 * @proxy: A #MuxOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @arg_property_name: Argument to pass with the method invocation.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_properties_call_set_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_properties_call_set_sync() for the synchronous, blocking version of this method.
 */
void
        mux_org_freedesktop_dbus_properties_call_set (
        MuxOrgFreedesktopDBusProperties *proxy,
        const gchar *arg_interface_name,
const gchar *arg_property_name,
        GVariant *arg_value,
GCancellable *cancellable,
        GAsyncReadyCallback callback,
gpointer user_data)
{
g_dbus_proxy_call (G_DBUS_PROXY (proxy),
"Set",
g_variant_new ("(ss@v)",
arg_interface_name,
arg_property_name,
arg_value),
G_DBUS_CALL_FLAGS_NONE,
-1,
cancellable,
callback,
user_data);
}

/**
 * mux_org_freedesktop_dbus_properties_call_set_finish:
 * @proxy: A #MuxOrgFreedesktopDBusPropertiesProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_properties_call_set().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_properties_call_set().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_freedesktop_dbus_properties_call_set_finish (
        MuxOrgFreedesktopDBusProperties *proxy,
        GAsyncResult *res,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "()");
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_properties_call_set_sync:
 * @proxy: A #MuxOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @arg_property_name: Argument to pass with the method invocation.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_properties_call_set() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
        mux_org_freedesktop_dbus_properties_call_set_sync (
        MuxOrgFreedesktopDBusProperties *proxy,
        const gchar *arg_interface_name,
const gchar *arg_property_name,
        GVariant *arg_value,
GCancellable *cancellable,
        GError **error)
{
GVariant *_ret;
_ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
                               "Set",
                               g_variant_new ("(ss@v)",
                                              arg_interface_name,
                                              arg_property_name,
                                              arg_value),
                               G_DBUS_CALL_FLAGS_NONE,
                               -1,
                               cancellable,
                               error);
if (_ret == NULL)
goto _out;
g_variant_get (_ret,
"()");
g_variant_unref (_ret);
_out:
return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_properties_complete_get:
 * @object: A #MuxOrgFreedesktopDBusProperties.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @value: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mux_org_freedesktop_dbus_properties_complete_get (
        MuxOrgFreedesktopDBusProperties *object,
        GDBusMethodInvocation *invocation,
        GVariant *value)
{
    g_dbus_method_invocation_return_value (invocation,
                                           g_variant_new ("(@v)",
                                                          value));
}

/**
 * mux_org_freedesktop_dbus_properties_complete_get_all:
 * @object: A #MuxOrgFreedesktopDBusProperties.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @properties: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mux_org_freedesktop_dbus_properties_complete_get_all (
        MuxOrgFreedesktopDBusProperties *object,
        GDBusMethodInvocation *invocation,
        GVariant *properties)
{
    g_dbus_method_invocation_return_value (invocation,
                                           g_variant_new ("(@a{sv})",
                                                          properties));
}

/**
 * mux_org_freedesktop_dbus_properties_complete_set:
 * @object: A #MuxOrgFreedesktopDBusProperties.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mux_org_freedesktop_dbus_properties_complete_set (
        MuxOrgFreedesktopDBusProperties *object,
        GDBusMethodInvocation *invocation)
{
    g_dbus_method_invocation_return_value (invocation,
                                           g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * MuxOrgFreedesktopDBusPropertiesProxy:
 *
 * The #MuxOrgFreedesktopDBusPropertiesProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MuxOrgFreedesktopDBusPropertiesProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MuxOrgFreedesktopDBusPropertiesProxy.
 */

struct _MuxOrgFreedesktopDBusPropertiesProxyPrivate
{
    GData *qdata;
};

static void mux_org_freedesktop_dbus_properties_proxy_iface_init (MuxOrgFreedesktopDBusPropertiesIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusPropertiesProxy, mux_org_freedesktop_dbus_properties_proxy, G_TYPE_DBUS_PROXY,
        G_ADD_PRIVATE (MuxOrgFreedesktopDBusPropertiesProxy)
G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, mux_org_freedesktop_dbus_properties_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusPropertiesProxy, mux_org_freedesktop_dbus_properties_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, mux_org_freedesktop_dbus_properties_proxy_iface_init));

#endif
static void
mux_org_freedesktop_dbus_properties_proxy_finalize (GObject *object)
{
    MuxOrgFreedesktopDBusPropertiesProxy *proxy = MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY (object);
    g_datalist_clear (&proxy->priv->qdata);
    G_OBJECT_CLASS (mux_org_freedesktop_dbus_properties_proxy_parent_class)->finalize (object);
}

static void
mux_org_freedesktop_dbus_properties_proxy_get_property (GObject      *object,
                                                        guint         prop_id,
                                                        GValue       *value,
                                                        GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mux_org_freedesktop_dbus_properties_proxy_set_property (GObject      *object,
                                                        guint         prop_id,
                                                        const GValue *value,
                                                        GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mux_org_freedesktop_dbus_properties_proxy_g_signal (GDBusProxy *proxy,
                                                    const gchar *sender_name G_GNUC_UNUSED,
                                                    const gchar *signal_name,
                                                    GVariant *parameters)
{
    _ExtendedGDBusSignalInfo *info;
    GVariantIter iter;
    GVariant *child;
    GValue *paramv;
    guint num_params;
    guint n;
    guint signal_id;
    info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_properties_interface_info.parent_struct, signal_name);
    if (info == NULL)
        return;
    num_params = g_variant_n_children (parameters);
    paramv = g_new0 (GValue, num_params + 1);
    g_value_init (&paramv[0], MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
    g_value_set_object (&paramv[0], proxy);
    g_variant_iter_init (&iter, parameters);
    n = 1;
    while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
        _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
        if (arg_info->use_gvariant)
        {
            g_value_init (&paramv[n], G_TYPE_VARIANT);
            g_value_set_variant (&paramv[n], child);
            n++;
        }
        else
            g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
        g_variant_unref (child);
    }
    signal_id = g_signal_lookup (info->signal_name, MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
    g_signal_emitv (paramv, signal_id, 0, NULL);
    for (n = 0; n < num_params + 1; n++)
        g_value_unset (&paramv[n]);
    g_free (paramv);
}

static void
mux_org_freedesktop_dbus_properties_proxy_g_properties_changed (GDBusProxy *_proxy,
                                                                GVariant *changed_properties,
                                                                const gchar *const *invalidated_properties)
{
    MuxOrgFreedesktopDBusPropertiesProxy *proxy = MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY (_proxy);
    guint n;
    const gchar *key;
    GVariantIter *iter;
    _ExtendedGDBusPropertyInfo *info;
    g_variant_get (changed_properties, "a{sv}", &iter);
    while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
        info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_properties_interface_info.parent_struct, key);
        g_datalist_remove_data (&proxy->priv->qdata, key);
        if (info != NULL)
            g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
    g_variant_iter_free (iter);
    for (n = 0; invalidated_properties[n] != NULL; n++)
    {
        info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_properties_interface_info.parent_struct, invalidated_properties[n]);
        g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
        if (info != NULL)
            g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
mux_org_freedesktop_dbus_properties_proxy_init (MuxOrgFreedesktopDBusPropertiesProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
    proxy->priv = mux_org_freedesktop_dbus_properties_proxy_get_instance_private (proxy);
#else
    proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, MuxOrgFreedesktopDBusPropertiesProxyPrivate);
#endif

    g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), mux_org_freedesktop_dbus_properties_interface_info ());
}

static void
mux_org_freedesktop_dbus_properties_proxy_class_init (MuxOrgFreedesktopDBusPropertiesProxyClass *klass)
{
    GObjectClass *gobject_class;
    GDBusProxyClass *proxy_class;

    gobject_class = G_OBJECT_CLASS (klass);
    gobject_class->finalize     = mux_org_freedesktop_dbus_properties_proxy_finalize;
    gobject_class->get_property = mux_org_freedesktop_dbus_properties_proxy_get_property;
    gobject_class->set_property = mux_org_freedesktop_dbus_properties_proxy_set_property;

    proxy_class = G_DBUS_PROXY_CLASS (klass);
    proxy_class->g_signal = mux_org_freedesktop_dbus_properties_proxy_g_signal;
    proxy_class->g_properties_changed = mux_org_freedesktop_dbus_properties_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
    g_type_class_add_private (klass, sizeof (MuxOrgFreedesktopDBusPropertiesProxyPrivate));
#endif
}

static void
mux_org_freedesktop_dbus_properties_proxy_iface_init (MuxOrgFreedesktopDBusPropertiesIface *iface)
{
}

/**
 * mux_org_freedesktop_dbus_properties_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_properties_proxy_new_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_properties_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
mux_org_freedesktop_dbus_properties_proxy_new (
        GDBusConnection     *connection,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GAsyncReadyCallback  callback,
        gpointer             user_data)
{
    g_async_initable_new_async (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
}

/**
 * mux_org_freedesktop_dbus_properties_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_properties_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_properties_proxy_new().
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusProperties *
mux_org_freedesktop_dbus_properties_proxy_new_finish (
        GAsyncResult        *res,
        GError             **error)
{
    GObject *ret;
    GObject *source_object;
    source_object = g_async_result_get_source_object (res);
    ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
    g_object_unref (source_object);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
    else
        return NULL;
}

/**
 * mux_org_freedesktop_dbus_properties_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_properties_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusProperties *
mux_org_freedesktop_dbus_properties_proxy_new_sync (
        GDBusConnection     *connection,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GError             **error)
{
    GInitable *ret;
    ret = g_initable_new (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
    else
        return NULL;
}


/**
 * mux_org_freedesktop_dbus_properties_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like mux_org_freedesktop_dbus_properties_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_properties_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_properties_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
mux_org_freedesktop_dbus_properties_proxy_new_for_bus (
        GBusType             bus_type,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GAsyncReadyCallback  callback,
        gpointer             user_data)
{
    g_async_initable_new_async (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
}

/**
 * mux_org_freedesktop_dbus_properties_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_properties_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_properties_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusProperties *
mux_org_freedesktop_dbus_properties_proxy_new_for_bus_finish (
        GAsyncResult        *res,
        GError             **error)
{
    GObject *ret;
    GObject *source_object;
    source_object = g_async_result_get_source_object (res);
    ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
    g_object_unref (source_object);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
    else
        return NULL;
}

/**
 * mux_org_freedesktop_dbus_properties_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like mux_org_freedesktop_dbus_properties_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_properties_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusProperties *
mux_org_freedesktop_dbus_properties_proxy_new_for_bus_sync (
        GBusType             bus_type,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GError             **error)
{
    GInitable *ret;
    ret = g_initable_new (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
    else
        return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * MuxOrgFreedesktopDBusPropertiesSkeleton:
 *
 * The #MuxOrgFreedesktopDBusPropertiesSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MuxOrgFreedesktopDBusPropertiesSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MuxOrgFreedesktopDBusPropertiesSkeleton.
 */

struct _MuxOrgFreedesktopDBusPropertiesSkeletonPrivate
{
    GValue *properties;
    GList *changed_properties;
    GSource *changed_properties_idle_source;
    GMainContext *context;
    GMutex lock;
};

static void
_mux_org_freedesktop_dbus_properties_skeleton_handle_method_call (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name,
        const gchar *method_name,
        GVariant *parameters,
        GDBusMethodInvocation *invocation,
        gpointer user_data)
{
    MuxOrgFreedesktopDBusPropertiesSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (user_data);
    _ExtendedGDBusMethodInfo *info;
    GVariantIter iter;
    GVariant *child;
    GValue *paramv;
    guint num_params;
    guint num_extra;
    guint n;
    guint signal_id;
    GValue return_value = G_VALUE_INIT;
    info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
    g_assert (info != NULL);
    num_params = g_variant_n_children (parameters);
    num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
    n = 0;
    g_value_init (&paramv[n], MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
    g_value_set_object (&paramv[n++], skeleton);
    g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
    g_value_set_object (&paramv[n++], invocation);
    if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
        g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
        g_assert_not_reached ();
#endif
    }
    g_variant_iter_init (&iter, parameters);
    while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
        _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
        if (arg_info->use_gvariant)
        {
            g_value_init (&paramv[n], G_TYPE_VARIANT);
            g_value_set_variant (&paramv[n], child);
            n++;
        }
        else
            g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
        g_variant_unref (child);
    }
    signal_id = g_signal_lookup (info->signal_name, MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
    g_value_init (&return_value, G_TYPE_BOOLEAN);
    g_signal_emitv (paramv, signal_id, 0, &return_value);
    if (!g_value_get_boolean (&return_value))
        g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
    g_value_unset (&return_value);
    for (n = 0; n < num_params + num_extra; n++)
        g_value_unset (&paramv[n]);
    g_free (paramv);
}

static GVariant *
_mux_org_freedesktop_dbus_properties_skeleton_handle_get_property (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name G_GNUC_UNUSED,
        const gchar *property_name,
        GError **error,
        gpointer user_data)
{
    MuxOrgFreedesktopDBusPropertiesSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (user_data);
    GValue value = G_VALUE_INIT;
    GParamSpec *pspec;
    _ExtendedGDBusPropertyInfo *info;
    GVariant *ret;
    ret = NULL;
    info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_properties_interface_info.parent_struct, property_name);
    g_assert (info != NULL);
    pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
    if (pspec == NULL)
    {
        g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
    else
    {
        g_value_init (&value, pspec->value_type);
        g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
        ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
        g_value_unset (&value);
    }
    return ret;
}

static gboolean
_mux_org_freedesktop_dbus_properties_skeleton_handle_set_property (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name G_GNUC_UNUSED,
        const gchar *property_name,
        GVariant *variant,
        GError **error,
        gpointer user_data)
{
    MuxOrgFreedesktopDBusPropertiesSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (user_data);
    GValue value = G_VALUE_INIT;
    GParamSpec *pspec;
    _ExtendedGDBusPropertyInfo *info;
    gboolean ret;
    ret = FALSE;
    info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_properties_interface_info.parent_struct, property_name);
    g_assert (info != NULL);
    pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
    if (pspec == NULL)
    {
        g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
    else
    {
        if (info->use_gvariant)
            g_value_set_variant (&value, variant);
        else
            g_dbus_gvariant_to_gvalue (variant, &value);
        g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
        g_value_unset (&value);
        ret = TRUE;
    }
    return ret;
}

static const GDBusInterfaceVTable _mux_org_freedesktop_dbus_properties_skeleton_vtable =
        {
                _mux_org_freedesktop_dbus_properties_skeleton_handle_method_call,
                _mux_org_freedesktop_dbus_properties_skeleton_handle_get_property,
                _mux_org_freedesktop_dbus_properties_skeleton_handle_set_property,
                {NULL}
        };

static GDBusInterfaceInfo *
mux_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
    return mux_org_freedesktop_dbus_properties_interface_info ();
}

static GDBusInterfaceVTable *
mux_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
    return (GDBusInterfaceVTable *) &_mux_org_freedesktop_dbus_properties_skeleton_vtable;
}

static GVariant *
mux_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
    MuxOrgFreedesktopDBusPropertiesSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (_skeleton);

    GVariantBuilder builder;
    guint n;
    g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
    if (_mux_org_freedesktop_dbus_properties_interface_info.parent_struct.properties == NULL)
        goto out;
    for (n = 0; _mux_org_freedesktop_dbus_properties_interface_info.parent_struct.properties[n] != NULL; n++)
    {
        GDBusPropertyInfo *info = _mux_org_freedesktop_dbus_properties_interface_info.parent_struct.properties[n];
        if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
            GVariant *value;
            value = _mux_org_freedesktop_dbus_properties_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Properties", info->name, NULL, skeleton);
            if (value != NULL)
            {
                g_variant_take_ref (value);
                g_variant_builder_add (&builder, "{sv}", info->name, value);
                g_variant_unref (value);
            }
        }
    }
    out:
    return g_variant_builder_end (&builder);
}

static void
mux_org_freedesktop_dbus_properties_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
        _mux_org_freedesktop_dbus_properties_on_signal_properties_changed (
        MuxOrgFreedesktopDBusProperties *object,
        const gchar *arg_interface_name,
        GVariant *arg_changed_properties,
const gchar *const *arg_invalidated_properties)
{
MuxOrgFreedesktopDBusPropertiesSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (object);

GList      *connections, *l;
GVariant   *signal_variant;
connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

signal_variant = g_variant_ref_sink (g_variant_new ("(s@a{sv}^as)",
                                                    arg_interface_name,
                                                    arg_changed_properties,
                                                    arg_invalidated_properties));
for (l = connections; l != NULL; l = l->next)
{
GDBusConnection *connection = l->data;
g_dbus_connection_emit_signal (connection,
NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Properties", "PropertiesChanged",
signal_variant, NULL);
}
g_variant_unref (signal_variant);
g_list_free_full (connections, g_object_unref);
}

static void mux_org_freedesktop_dbus_properties_skeleton_iface_init (MuxOrgFreedesktopDBusPropertiesIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusPropertiesSkeleton, mux_org_freedesktop_dbus_properties_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
        G_ADD_PRIVATE (MuxOrgFreedesktopDBusPropertiesSkeleton)
G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, mux_org_freedesktop_dbus_properties_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusPropertiesSkeleton, mux_org_freedesktop_dbus_properties_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, mux_org_freedesktop_dbus_properties_skeleton_iface_init));

#endif
static void
mux_org_freedesktop_dbus_properties_skeleton_finalize (GObject *object)
{
    MuxOrgFreedesktopDBusPropertiesSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (object);
    g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
    if (skeleton->priv->changed_properties_idle_source != NULL)
        g_source_destroy (skeleton->priv->changed_properties_idle_source);
    g_main_context_unref (skeleton->priv->context);
    g_mutex_clear (&skeleton->priv->lock);
    G_OBJECT_CLASS (mux_org_freedesktop_dbus_properties_skeleton_parent_class)->finalize (object);
}

static void
mux_org_freedesktop_dbus_properties_skeleton_init (MuxOrgFreedesktopDBusPropertiesSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
    skeleton->priv = mux_org_freedesktop_dbus_properties_skeleton_get_instance_private (skeleton);
#else
    skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON, MuxOrgFreedesktopDBusPropertiesSkeletonPrivate);
#endif

    g_mutex_init (&skeleton->priv->lock);
    skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
mux_org_freedesktop_dbus_properties_skeleton_class_init (MuxOrgFreedesktopDBusPropertiesSkeletonClass *klass)
{
    GObjectClass *gobject_class;
    GDBusInterfaceSkeletonClass *skeleton_class;

    gobject_class = G_OBJECT_CLASS (klass);
    gobject_class->finalize = mux_org_freedesktop_dbus_properties_skeleton_finalize;

    skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
    skeleton_class->get_info = mux_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_info;
    skeleton_class->get_properties = mux_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_properties;
    skeleton_class->flush = mux_org_freedesktop_dbus_properties_skeleton_dbus_interface_flush;
    skeleton_class->get_vtable = mux_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
    g_type_class_add_private (klass, sizeof (MuxOrgFreedesktopDBusPropertiesSkeletonPrivate));
#endif
}

static void
mux_org_freedesktop_dbus_properties_skeleton_iface_init (MuxOrgFreedesktopDBusPropertiesIface *iface)
{
    iface->properties_changed = _mux_org_freedesktop_dbus_properties_on_signal_properties_changed;
}

/**
 * mux_org_freedesktop_dbus_properties_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>.
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPropertiesSkeleton): The skeleton object.
 */
MuxOrgFreedesktopDBusProperties *
mux_org_freedesktop_dbus_properties_skeleton_new (void)
{
    return MUX_ORG_FREEDESKTOP_DBUS_PROPERTIES (g_object_new (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.DBus.Introspectable
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:MuxOrgFreedesktopDBusIntrospectable
 * @title: MuxOrgFreedesktopDBusIntrospectable
 * @short_description: Generated C code for the org.freedesktop.DBus.Introspectable D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.DBus.Introspectable ---- */

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_xml_data =
        {
                {
                        -1,
                        (gchar *) "xml_data",
                        (gchar *) "s",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_pointers[] =
        {
                &_mux_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_xml_data,
                NULL
        };

static const _ExtendedGDBusMethodInfo _mux_org_freedesktop_dbus_introspectable_method_info_introspect =
        {
                {
                        -1,
                        (gchar *) "Introspect",
                        NULL,
                        (GDBusArgInfo **) &_mux_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_pointers,
                        NULL
                },
                "handle-introspect",
                FALSE
        };

static const _ExtendedGDBusMethodInfo * const _mux_org_freedesktop_dbus_introspectable_method_info_pointers[] =
        {
                &_mux_org_freedesktop_dbus_introspectable_method_info_introspect,
                NULL
        };

static const _ExtendedGDBusInterfaceInfo _mux_org_freedesktop_dbus_introspectable_interface_info =
        {
                {
                        -1,
                        (gchar *) "org.freedesktop.DBus.Introspectable",
                        (GDBusMethodInfo **) &_mux_org_freedesktop_dbus_introspectable_method_info_pointers,
                        NULL,
                        NULL,
                        NULL
                },
                "org-freedesktop-dbus-introspectable",
        };


/**
 * mux_org_freedesktop_dbus_introspectable_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
mux_org_freedesktop_dbus_introspectable_interface_info (void)
{
    return (GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_introspectable_interface_info.parent_struct;
}

/**
 * mux_org_freedesktop_dbus_introspectable_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #MuxOrgFreedesktopDBusIntrospectable interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
mux_org_freedesktop_dbus_introspectable_override_properties (GObjectClass *klass, guint property_id_begin)
{
    return property_id_begin - 1;
}



/**
 * MuxOrgFreedesktopDBusIntrospectable:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>.
 */

/**
 * MuxOrgFreedesktopDBusIntrospectableIface:
 * @parent_iface: The parent interface.
 * @handle_introspect: Handler for the #MuxOrgFreedesktopDBusIntrospectable::handle-introspect signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>.
 */

typedef MuxOrgFreedesktopDBusIntrospectableIface MuxOrgFreedesktopDBusIntrospectableInterface;
G_DEFINE_INTERFACE (MuxOrgFreedesktopDBusIntrospectable, mux_org_freedesktop_dbus_introspectable, G_TYPE_OBJECT);

static void
mux_org_freedesktop_dbus_introspectable_default_init (MuxOrgFreedesktopDBusIntrospectableIface *iface)
{
    /* GObject signals for incoming D-Bus method calls: */
    /**
     * MuxOrgFreedesktopDBusIntrospectable::handle-introspect:
     * @object: A #MuxOrgFreedesktopDBusIntrospectable.
     * @invocation: A #GDBusMethodInvocation.
     *
     * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method.
     *
     * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mux_org_freedesktop_dbus_introspectable_complete_introspect() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
     *
     * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
     */
    g_signal_new ("handle-introspect",
                  G_TYPE_FROM_INTERFACE (iface),
                  G_SIGNAL_RUN_LAST,
                  G_STRUCT_OFFSET (MuxOrgFreedesktopDBusIntrospectableIface, handle_introspect),
                  g_signal_accumulator_true_handled,
                  NULL,
                  g_cclosure_marshal_generic,
                  G_TYPE_BOOLEAN,
                  1,
                  G_TYPE_DBUS_METHOD_INVOCATION);

}

/**
 * mux_org_freedesktop_dbus_introspectable_call_introspect:
 * @proxy: A #MuxOrgFreedesktopDBusIntrospectableProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_introspectable_call_introspect_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_introspectable_call_introspect_sync() for the synchronous, blocking version of this method.
 */
void
        mux_org_freedesktop_dbus_introspectable_call_introspect (
        MuxOrgFreedesktopDBusIntrospectable *proxy,
        GCancellable *cancellable,
        GAsyncReadyCallback callback,
gpointer user_data)
{
g_dbus_proxy_call (G_DBUS_PROXY (proxy),
"Introspect",
g_variant_new ("()"),
G_DBUS_CALL_FLAGS_NONE,
-1,
cancellable,
callback,
user_data);
}

/**
 * mux_org_freedesktop_dbus_introspectable_call_introspect_finish:
 * @proxy: A #MuxOrgFreedesktopDBusIntrospectableProxy.
 * @out_xml_data: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_introspectable_call_introspect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_introspectable_call_introspect().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_freedesktop_dbus_introspectable_call_introspect_finish (
        MuxOrgFreedesktopDBusIntrospectable *proxy,
        gchar **out_xml_data,
        GAsyncResult *res,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "(s)",
                   out_xml_data);
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_introspectable_call_introspect_sync:
 * @proxy: A #MuxOrgFreedesktopDBusIntrospectableProxy.
 * @out_xml_data: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_introspectable_call_introspect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_freedesktop_dbus_introspectable_call_introspect_sync (
        MuxOrgFreedesktopDBusIntrospectable *proxy,
        gchar **out_xml_data,
        GCancellable *cancellable,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
                                   "Introspect",
                                   g_variant_new ("()"),
                                   G_DBUS_CALL_FLAGS_NONE,
                                   -1,
                                   cancellable,
                                   error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "(s)",
                   out_xml_data);
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_introspectable_complete_introspect:
 * @object: A #MuxOrgFreedesktopDBusIntrospectable.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @xml_data: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
        mux_org_freedesktop_dbus_introspectable_complete_introspect (
        MuxOrgFreedesktopDBusIntrospectable *object,
        GDBusMethodInvocation *invocation,
        const gchar *xml_data)
{
g_dbus_method_invocation_return_value (invocation,
        g_variant_new ("(s)",
                       xml_data));
}

/* ------------------------------------------------------------------------ */

/**
 * MuxOrgFreedesktopDBusIntrospectableProxy:
 *
 * The #MuxOrgFreedesktopDBusIntrospectableProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MuxOrgFreedesktopDBusIntrospectableProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MuxOrgFreedesktopDBusIntrospectableProxy.
 */

struct _MuxOrgFreedesktopDBusIntrospectableProxyPrivate
{
    GData *qdata;
};

static void mux_org_freedesktop_dbus_introspectable_proxy_iface_init (MuxOrgFreedesktopDBusIntrospectableIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusIntrospectableProxy, mux_org_freedesktop_dbus_introspectable_proxy, G_TYPE_DBUS_PROXY,
        G_ADD_PRIVATE (MuxOrgFreedesktopDBusIntrospectableProxy)
G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, mux_org_freedesktop_dbus_introspectable_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusIntrospectableProxy, mux_org_freedesktop_dbus_introspectable_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, mux_org_freedesktop_dbus_introspectable_proxy_iface_init));

#endif
static void
mux_org_freedesktop_dbus_introspectable_proxy_finalize (GObject *object)
{
    MuxOrgFreedesktopDBusIntrospectableProxy *proxy = MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY (object);
    g_datalist_clear (&proxy->priv->qdata);
    G_OBJECT_CLASS (mux_org_freedesktop_dbus_introspectable_proxy_parent_class)->finalize (object);
}

static void
mux_org_freedesktop_dbus_introspectable_proxy_get_property (GObject      *object,
                                                            guint         prop_id,
                                                            GValue       *value,
                                                            GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mux_org_freedesktop_dbus_introspectable_proxy_set_property (GObject      *object,
                                                            guint         prop_id,
                                                            const GValue *value,
                                                            GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mux_org_freedesktop_dbus_introspectable_proxy_g_signal (GDBusProxy *proxy,
                                                        const gchar *sender_name G_GNUC_UNUSED,
                                                        const gchar *signal_name,
                                                        GVariant *parameters)
{
    _ExtendedGDBusSignalInfo *info;
    GVariantIter iter;
    GVariant *child;
    GValue *paramv;
    guint num_params;
    guint n;
    guint signal_id;
    info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_introspectable_interface_info.parent_struct, signal_name);
    if (info == NULL)
        return;
    num_params = g_variant_n_children (parameters);
    paramv = g_new0 (GValue, num_params + 1);
    g_value_init (&paramv[0], MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
    g_value_set_object (&paramv[0], proxy);
    g_variant_iter_init (&iter, parameters);
    n = 1;
    while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
        _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
        if (arg_info->use_gvariant)
        {
            g_value_init (&paramv[n], G_TYPE_VARIANT);
            g_value_set_variant (&paramv[n], child);
            n++;
        }
        else
            g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
        g_variant_unref (child);
    }
    signal_id = g_signal_lookup (info->signal_name, MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
    g_signal_emitv (paramv, signal_id, 0, NULL);
    for (n = 0; n < num_params + 1; n++)
        g_value_unset (&paramv[n]);
    g_free (paramv);
}

static void
mux_org_freedesktop_dbus_introspectable_proxy_g_properties_changed (GDBusProxy *_proxy,
                                                                    GVariant *changed_properties,
                                                                    const gchar *const *invalidated_properties)
{
    MuxOrgFreedesktopDBusIntrospectableProxy *proxy = MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY (_proxy);
    guint n;
    const gchar *key;
    GVariantIter *iter;
    _ExtendedGDBusPropertyInfo *info;
    g_variant_get (changed_properties, "a{sv}", &iter);
    while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
        info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_introspectable_interface_info.parent_struct, key);
        g_datalist_remove_data (&proxy->priv->qdata, key);
        if (info != NULL)
            g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
    g_variant_iter_free (iter);
    for (n = 0; invalidated_properties[n] != NULL; n++)
    {
        info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_introspectable_interface_info.parent_struct, invalidated_properties[n]);
        g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
        if (info != NULL)
            g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
mux_org_freedesktop_dbus_introspectable_proxy_init (MuxOrgFreedesktopDBusIntrospectableProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
    proxy->priv = mux_org_freedesktop_dbus_introspectable_proxy_get_instance_private (proxy);
#else
    proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, MuxOrgFreedesktopDBusIntrospectableProxyPrivate);
#endif

    g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), mux_org_freedesktop_dbus_introspectable_interface_info ());
}

static void
mux_org_freedesktop_dbus_introspectable_proxy_class_init (MuxOrgFreedesktopDBusIntrospectableProxyClass *klass)
{
    GObjectClass *gobject_class;
    GDBusProxyClass *proxy_class;

    gobject_class = G_OBJECT_CLASS (klass);
    gobject_class->finalize     = mux_org_freedesktop_dbus_introspectable_proxy_finalize;
    gobject_class->get_property = mux_org_freedesktop_dbus_introspectable_proxy_get_property;
    gobject_class->set_property = mux_org_freedesktop_dbus_introspectable_proxy_set_property;

    proxy_class = G_DBUS_PROXY_CLASS (klass);
    proxy_class->g_signal = mux_org_freedesktop_dbus_introspectable_proxy_g_signal;
    proxy_class->g_properties_changed = mux_org_freedesktop_dbus_introspectable_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
    g_type_class_add_private (klass, sizeof (MuxOrgFreedesktopDBusIntrospectableProxyPrivate));
#endif
}

static void
mux_org_freedesktop_dbus_introspectable_proxy_iface_init (MuxOrgFreedesktopDBusIntrospectableIface *iface)
{
}

/**
 * mux_org_freedesktop_dbus_introspectable_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_introspectable_proxy_new_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_introspectable_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
mux_org_freedesktop_dbus_introspectable_proxy_new (
        GDBusConnection     *connection,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GAsyncReadyCallback  callback,
        gpointer             user_data)
{
    g_async_initable_new_async (MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
}

/**
 * mux_org_freedesktop_dbus_introspectable_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_introspectable_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_introspectable_proxy_new().
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusIntrospectable *
mux_org_freedesktop_dbus_introspectable_proxy_new_finish (
        GAsyncResult        *res,
        GError             **error)
{
    GObject *ret;
    GObject *source_object;
    source_object = g_async_result_get_source_object (res);
    ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
    g_object_unref (source_object);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
    else
        return NULL;
}

/**
 * mux_org_freedesktop_dbus_introspectable_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_introspectable_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusIntrospectable *
mux_org_freedesktop_dbus_introspectable_proxy_new_sync (
        GDBusConnection     *connection,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GError             **error)
{
    GInitable *ret;
    ret = g_initable_new (MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
    else
        return NULL;
}


/**
 * mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like mux_org_freedesktop_dbus_introspectable_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus (
        GBusType             bus_type,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GAsyncReadyCallback  callback,
        gpointer             user_data)
{
    g_async_initable_new_async (MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
}

/**
 * mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusIntrospectable *
mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus_finish (
        GAsyncResult        *res,
        GError             **error)
{
    GObject *ret;
    GObject *source_object;
    source_object = g_async_result_get_source_object (res);
    ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
    g_object_unref (source_object);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
    else
        return NULL;
}

/**
 * mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like mux_org_freedesktop_dbus_introspectable_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusIntrospectable *
mux_org_freedesktop_dbus_introspectable_proxy_new_for_bus_sync (
        GBusType             bus_type,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GError             **error)
{
    GInitable *ret;
    ret = g_initable_new (MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
    else
        return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * MuxOrgFreedesktopDBusIntrospectableSkeleton:
 *
 * The #MuxOrgFreedesktopDBusIntrospectableSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MuxOrgFreedesktopDBusIntrospectableSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MuxOrgFreedesktopDBusIntrospectableSkeleton.
 */

struct _MuxOrgFreedesktopDBusIntrospectableSkeletonPrivate
{
    GValue *properties;
    GList *changed_properties;
    GSource *changed_properties_idle_source;
    GMainContext *context;
    GMutex lock;
};

static void
_mux_org_freedesktop_dbus_introspectable_skeleton_handle_method_call (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name,
        const gchar *method_name,
        GVariant *parameters,
        GDBusMethodInvocation *invocation,
        gpointer user_data)
{
    MuxOrgFreedesktopDBusIntrospectableSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (user_data);
    _ExtendedGDBusMethodInfo *info;
    GVariantIter iter;
    GVariant *child;
    GValue *paramv;
    guint num_params;
    guint num_extra;
    guint n;
    guint signal_id;
    GValue return_value = G_VALUE_INIT;
    info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
    g_assert (info != NULL);
    num_params = g_variant_n_children (parameters);
    num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
    n = 0;
    g_value_init (&paramv[n], MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
    g_value_set_object (&paramv[n++], skeleton);
    g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
    g_value_set_object (&paramv[n++], invocation);
    if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
        g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
        g_assert_not_reached ();
#endif
    }
    g_variant_iter_init (&iter, parameters);
    while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
        _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
        if (arg_info->use_gvariant)
        {
            g_value_init (&paramv[n], G_TYPE_VARIANT);
            g_value_set_variant (&paramv[n], child);
            n++;
        }
        else
            g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
        g_variant_unref (child);
    }
    signal_id = g_signal_lookup (info->signal_name, MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
    g_value_init (&return_value, G_TYPE_BOOLEAN);
    g_signal_emitv (paramv, signal_id, 0, &return_value);
    if (!g_value_get_boolean (&return_value))
        g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
    g_value_unset (&return_value);
    for (n = 0; n < num_params + num_extra; n++)
        g_value_unset (&paramv[n]);
    g_free (paramv);
}

static GVariant *
_mux_org_freedesktop_dbus_introspectable_skeleton_handle_get_property (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name G_GNUC_UNUSED,
        const gchar *property_name,
        GError **error,
        gpointer user_data)
{
    MuxOrgFreedesktopDBusIntrospectableSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (user_data);
    GValue value = G_VALUE_INIT;
    GParamSpec *pspec;
    _ExtendedGDBusPropertyInfo *info;
    GVariant *ret;
    ret = NULL;
    info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_introspectable_interface_info.parent_struct, property_name);
    g_assert (info != NULL);
    pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
    if (pspec == NULL)
    {
        g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
    else
    {
        g_value_init (&value, pspec->value_type);
        g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
        ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
        g_value_unset (&value);
    }
    return ret;
}

static gboolean
_mux_org_freedesktop_dbus_introspectable_skeleton_handle_set_property (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name G_GNUC_UNUSED,
        const gchar *property_name,
        GVariant *variant,
        GError **error,
        gpointer user_data)
{
    MuxOrgFreedesktopDBusIntrospectableSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (user_data);
    GValue value = G_VALUE_INIT;
    GParamSpec *pspec;
    _ExtendedGDBusPropertyInfo *info;
    gboolean ret;
    ret = FALSE;
    info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_introspectable_interface_info.parent_struct, property_name);
    g_assert (info != NULL);
    pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
    if (pspec == NULL)
    {
        g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
    else
    {
        if (info->use_gvariant)
            g_value_set_variant (&value, variant);
        else
            g_dbus_gvariant_to_gvalue (variant, &value);
        g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
        g_value_unset (&value);
        ret = TRUE;
    }
    return ret;
}

static const GDBusInterfaceVTable _mux_org_freedesktop_dbus_introspectable_skeleton_vtable =
        {
                _mux_org_freedesktop_dbus_introspectable_skeleton_handle_method_call,
                _mux_org_freedesktop_dbus_introspectable_skeleton_handle_get_property,
                _mux_org_freedesktop_dbus_introspectable_skeleton_handle_set_property,
                {NULL}
        };

static GDBusInterfaceInfo *
mux_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
    return mux_org_freedesktop_dbus_introspectable_interface_info ();
}

static GDBusInterfaceVTable *
mux_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
    return (GDBusInterfaceVTable *) &_mux_org_freedesktop_dbus_introspectable_skeleton_vtable;
}

static GVariant *
mux_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
    MuxOrgFreedesktopDBusIntrospectableSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (_skeleton);

    GVariantBuilder builder;
    guint n;
    g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
    if (_mux_org_freedesktop_dbus_introspectable_interface_info.parent_struct.properties == NULL)
        goto out;
    for (n = 0; _mux_org_freedesktop_dbus_introspectable_interface_info.parent_struct.properties[n] != NULL; n++)
    {
        GDBusPropertyInfo *info = _mux_org_freedesktop_dbus_introspectable_interface_info.parent_struct.properties[n];
        if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
            GVariant *value;
            value = _mux_org_freedesktop_dbus_introspectable_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Introspectable", info->name, NULL, skeleton);
            if (value != NULL)
            {
                g_variant_take_ref (value);
                g_variant_builder_add (&builder, "{sv}", info->name, value);
                g_variant_unref (value);
            }
        }
    }
    out:
    return g_variant_builder_end (&builder);
}

static void
mux_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void mux_org_freedesktop_dbus_introspectable_skeleton_iface_init (MuxOrgFreedesktopDBusIntrospectableIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusIntrospectableSkeleton, mux_org_freedesktop_dbus_introspectable_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
        G_ADD_PRIVATE (MuxOrgFreedesktopDBusIntrospectableSkeleton)
G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, mux_org_freedesktop_dbus_introspectable_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusIntrospectableSkeleton, mux_org_freedesktop_dbus_introspectable_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, mux_org_freedesktop_dbus_introspectable_skeleton_iface_init));

#endif
static void
mux_org_freedesktop_dbus_introspectable_skeleton_finalize (GObject *object)
{
    MuxOrgFreedesktopDBusIntrospectableSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (object);
    g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
    if (skeleton->priv->changed_properties_idle_source != NULL)
        g_source_destroy (skeleton->priv->changed_properties_idle_source);
    g_main_context_unref (skeleton->priv->context);
    g_mutex_clear (&skeleton->priv->lock);
    G_OBJECT_CLASS (mux_org_freedesktop_dbus_introspectable_skeleton_parent_class)->finalize (object);
}

static void
mux_org_freedesktop_dbus_introspectable_skeleton_init (MuxOrgFreedesktopDBusIntrospectableSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
    skeleton->priv = mux_org_freedesktop_dbus_introspectable_skeleton_get_instance_private (skeleton);
#else
    skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON, MuxOrgFreedesktopDBusIntrospectableSkeletonPrivate);
#endif

    g_mutex_init (&skeleton->priv->lock);
    skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
mux_org_freedesktop_dbus_introspectable_skeleton_class_init (MuxOrgFreedesktopDBusIntrospectableSkeletonClass *klass)
{
    GObjectClass *gobject_class;
    GDBusInterfaceSkeletonClass *skeleton_class;

    gobject_class = G_OBJECT_CLASS (klass);
    gobject_class->finalize = mux_org_freedesktop_dbus_introspectable_skeleton_finalize;

    skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
    skeleton_class->get_info = mux_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_info;
    skeleton_class->get_properties = mux_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_properties;
    skeleton_class->flush = mux_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_flush;
    skeleton_class->get_vtable = mux_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
    g_type_class_add_private (klass, sizeof (MuxOrgFreedesktopDBusIntrospectableSkeletonPrivate));
#endif
}

static void
mux_org_freedesktop_dbus_introspectable_skeleton_iface_init (MuxOrgFreedesktopDBusIntrospectableIface *iface)
{
}

/**
 * mux_org_freedesktop_dbus_introspectable_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>.
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusIntrospectableSkeleton): The skeleton object.
 */
MuxOrgFreedesktopDBusIntrospectable *
mux_org_freedesktop_dbus_introspectable_skeleton_new (void)
{
    return MUX_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (g_object_new (MUX_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.DBus.Peer
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:MuxOrgFreedesktopDBusPeer
 * @title: MuxOrgFreedesktopDBusPeer
 * @short_description: Generated C code for the org.freedesktop.DBus.Peer D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.DBus.Peer ---- */

static const _ExtendedGDBusMethodInfo _mux_org_freedesktop_dbus_peer_method_info_ping =
        {
                {
                        -1,
                        (gchar *) "Ping",
                        NULL,
                        NULL,
                        NULL
                },
                "handle-ping",
                FALSE
        };

static const _ExtendedGDBusArgInfo _mux_org_freedesktop_dbus_peer_method_info_get_machine_id_OUT_ARG_machine_uuid =
        {
                {
                        -1,
                        (gchar *) "machine_uuid",
                        (gchar *) "s",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_freedesktop_dbus_peer_method_info_get_machine_id_OUT_ARG_pointers[] =
        {
                &_mux_org_freedesktop_dbus_peer_method_info_get_machine_id_OUT_ARG_machine_uuid,
                NULL
        };

static const _ExtendedGDBusMethodInfo _mux_org_freedesktop_dbus_peer_method_info_get_machine_id =
        {
                {
                        -1,
                        (gchar *) "GetMachineId",
                        NULL,
                        (GDBusArgInfo **) &_mux_org_freedesktop_dbus_peer_method_info_get_machine_id_OUT_ARG_pointers,
                        NULL
                },
                "handle-get-machine-id",
                FALSE
        };

static const _ExtendedGDBusMethodInfo * const _mux_org_freedesktop_dbus_peer_method_info_pointers[] =
        {
                &_mux_org_freedesktop_dbus_peer_method_info_ping,
                &_mux_org_freedesktop_dbus_peer_method_info_get_machine_id,
                NULL
        };

static const _ExtendedGDBusInterfaceInfo _mux_org_freedesktop_dbus_peer_interface_info =
        {
                {
                        -1,
                        (gchar *) "org.freedesktop.DBus.Peer",
                        (GDBusMethodInfo **) &_mux_org_freedesktop_dbus_peer_method_info_pointers,
                        NULL,
                        NULL,
                        NULL
                },
                "org-freedesktop-dbus-peer",
        };


/**
 * mux_org_freedesktop_dbus_peer_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
mux_org_freedesktop_dbus_peer_interface_info (void)
{
    return (GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_peer_interface_info.parent_struct;
}

/**
 * mux_org_freedesktop_dbus_peer_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #MuxOrgFreedesktopDBusPeer interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
mux_org_freedesktop_dbus_peer_override_properties (GObjectClass *klass, guint property_id_begin)
{
    return property_id_begin - 1;
}



/**
 * MuxOrgFreedesktopDBusPeer:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>.
 */

/**
 * MuxOrgFreedesktopDBusPeerIface:
 * @parent_iface: The parent interface.
 * @handle_get_machine_id: Handler for the #MuxOrgFreedesktopDBusPeer::handle-get-machine-id signal.
 * @handle_ping: Handler for the #MuxOrgFreedesktopDBusPeer::handle-ping signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>.
 */

typedef MuxOrgFreedesktopDBusPeerIface MuxOrgFreedesktopDBusPeerInterface;
G_DEFINE_INTERFACE (MuxOrgFreedesktopDBusPeer, mux_org_freedesktop_dbus_peer, G_TYPE_OBJECT);

static void
mux_org_freedesktop_dbus_peer_default_init (MuxOrgFreedesktopDBusPeerIface *iface)
{
    /* GObject signals for incoming D-Bus method calls: */
    /**
     * MuxOrgFreedesktopDBusPeer::handle-ping:
     * @object: A #MuxOrgFreedesktopDBusPeer.
     * @invocation: A #GDBusMethodInvocation.
     *
     * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.Ping">Ping()</link> D-Bus method.
     *
     * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mux_org_freedesktop_dbus_peer_complete_ping() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
     *
     * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
     */
    g_signal_new ("handle-ping",
                  G_TYPE_FROM_INTERFACE (iface),
                  G_SIGNAL_RUN_LAST,
                  G_STRUCT_OFFSET (MuxOrgFreedesktopDBusPeerIface, handle_ping),
                  g_signal_accumulator_true_handled,
                  NULL,
                  g_cclosure_marshal_generic,
                  G_TYPE_BOOLEAN,
                  1,
                  G_TYPE_DBUS_METHOD_INVOCATION);

    /**
     * MuxOrgFreedesktopDBusPeer::handle-get-machine-id:
     * @object: A #MuxOrgFreedesktopDBusPeer.
     * @invocation: A #GDBusMethodInvocation.
     *
     * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.GetMachineId">GetMachineId()</link> D-Bus method.
     *
     * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mux_org_freedesktop_dbus_peer_complete_get_machine_id() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
     *
     * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
     */
    g_signal_new ("handle-get-machine-id",
                  G_TYPE_FROM_INTERFACE (iface),
                  G_SIGNAL_RUN_LAST,
                  G_STRUCT_OFFSET (MuxOrgFreedesktopDBusPeerIface, handle_get_machine_id),
                  g_signal_accumulator_true_handled,
                  NULL,
                  g_cclosure_marshal_generic,
                  G_TYPE_BOOLEAN,
                  1,
                  G_TYPE_DBUS_METHOD_INVOCATION);

}

/**
 * mux_org_freedesktop_dbus_peer_call_ping:
 * @proxy: A #MuxOrgFreedesktopDBusPeerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.Ping">Ping()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_peer_call_ping_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_peer_call_ping_sync() for the synchronous, blocking version of this method.
 */
void
        mux_org_freedesktop_dbus_peer_call_ping (
        MuxOrgFreedesktopDBusPeer *proxy,
        GCancellable *cancellable,
        GAsyncReadyCallback callback,
gpointer user_data)
{
g_dbus_proxy_call (G_DBUS_PROXY (proxy),
"Ping",
g_variant_new ("()"),
G_DBUS_CALL_FLAGS_NONE,
-1,
cancellable,
callback,
user_data);
}

/**
 * mux_org_freedesktop_dbus_peer_call_ping_finish:
 * @proxy: A #MuxOrgFreedesktopDBusPeerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_peer_call_ping().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_peer_call_ping().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_freedesktop_dbus_peer_call_ping_finish (
        MuxOrgFreedesktopDBusPeer *proxy,
        GAsyncResult *res,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "()");
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_peer_call_ping_sync:
 * @proxy: A #MuxOrgFreedesktopDBusPeerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.Ping">Ping()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_peer_call_ping() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_freedesktop_dbus_peer_call_ping_sync (
        MuxOrgFreedesktopDBusPeer *proxy,
        GCancellable *cancellable,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
                                   "Ping",
                                   g_variant_new ("()"),
                                   G_DBUS_CALL_FLAGS_NONE,
                                   -1,
                                   cancellable,
                                   error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "()");
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_peer_call_get_machine_id:
 * @proxy: A #MuxOrgFreedesktopDBusPeerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.GetMachineId">GetMachineId()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_peer_call_get_machine_id_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_peer_call_get_machine_id_sync() for the synchronous, blocking version of this method.
 */
void
        mux_org_freedesktop_dbus_peer_call_get_machine_id (
        MuxOrgFreedesktopDBusPeer *proxy,
        GCancellable *cancellable,
        GAsyncReadyCallback callback,
gpointer user_data)
{
g_dbus_proxy_call (G_DBUS_PROXY (proxy),
"GetMachineId",
g_variant_new ("()"),
G_DBUS_CALL_FLAGS_NONE,
-1,
cancellable,
callback,
user_data);
}

/**
 * mux_org_freedesktop_dbus_peer_call_get_machine_id_finish:
 * @proxy: A #MuxOrgFreedesktopDBusPeerProxy.
 * @out_machine_uuid: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_peer_call_get_machine_id().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_peer_call_get_machine_id().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_freedesktop_dbus_peer_call_get_machine_id_finish (
        MuxOrgFreedesktopDBusPeer *proxy,
        gchar **out_machine_uuid,
        GAsyncResult *res,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "(s)",
                   out_machine_uuid);
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_peer_call_get_machine_id_sync:
 * @proxy: A #MuxOrgFreedesktopDBusPeerProxy.
 * @out_machine_uuid: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.GetMachineId">GetMachineId()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_peer_call_get_machine_id() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_freedesktop_dbus_peer_call_get_machine_id_sync (
        MuxOrgFreedesktopDBusPeer *proxy,
        gchar **out_machine_uuid,
        GCancellable *cancellable,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
                                   "GetMachineId",
                                   g_variant_new ("()"),
                                   G_DBUS_CALL_FLAGS_NONE,
                                   -1,
                                   cancellable,
                                   error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "(s)",
                   out_machine_uuid);
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_freedesktop_dbus_peer_complete_ping:
 * @object: A #MuxOrgFreedesktopDBusPeer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.Ping">Ping()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mux_org_freedesktop_dbus_peer_complete_ping (
        MuxOrgFreedesktopDBusPeer *object,
        GDBusMethodInvocation *invocation)
{
    g_dbus_method_invocation_return_value (invocation,
                                           g_variant_new ("()"));
}

/**
 * mux_org_freedesktop_dbus_peer_complete_get_machine_id:
 * @object: A #MuxOrgFreedesktopDBusPeer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @machine_uuid: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.GetMachineId">GetMachineId()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
        mux_org_freedesktop_dbus_peer_complete_get_machine_id (
        MuxOrgFreedesktopDBusPeer *object,
        GDBusMethodInvocation *invocation,
        const gchar *machine_uuid)
{
g_dbus_method_invocation_return_value (invocation,
        g_variant_new ("(s)",
                       machine_uuid));
}

/* ------------------------------------------------------------------------ */

/**
 * MuxOrgFreedesktopDBusPeerProxy:
 *
 * The #MuxOrgFreedesktopDBusPeerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MuxOrgFreedesktopDBusPeerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MuxOrgFreedesktopDBusPeerProxy.
 */

struct _MuxOrgFreedesktopDBusPeerProxyPrivate
{
    GData *qdata;
};

static void mux_org_freedesktop_dbus_peer_proxy_iface_init (MuxOrgFreedesktopDBusPeerIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusPeerProxy, mux_org_freedesktop_dbus_peer_proxy, G_TYPE_DBUS_PROXY,
        G_ADD_PRIVATE (MuxOrgFreedesktopDBusPeerProxy)
G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER, mux_org_freedesktop_dbus_peer_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusPeerProxy, mux_org_freedesktop_dbus_peer_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER, mux_org_freedesktop_dbus_peer_proxy_iface_init));

#endif
static void
mux_org_freedesktop_dbus_peer_proxy_finalize (GObject *object)
{
    MuxOrgFreedesktopDBusPeerProxy *proxy = MUX_ORG_FREEDESKTOP_DBUS_PEER_PROXY (object);
    g_datalist_clear (&proxy->priv->qdata);
    G_OBJECT_CLASS (mux_org_freedesktop_dbus_peer_proxy_parent_class)->finalize (object);
}

static void
mux_org_freedesktop_dbus_peer_proxy_get_property (GObject      *object,
                                                  guint         prop_id,
                                                  GValue       *value,
                                                  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mux_org_freedesktop_dbus_peer_proxy_set_property (GObject      *object,
                                                  guint         prop_id,
                                                  const GValue *value,
                                                  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mux_org_freedesktop_dbus_peer_proxy_g_signal (GDBusProxy *proxy,
                                              const gchar *sender_name G_GNUC_UNUSED,
                                              const gchar *signal_name,
                                              GVariant *parameters)
{
    _ExtendedGDBusSignalInfo *info;
    GVariantIter iter;
    GVariant *child;
    GValue *paramv;
    guint num_params;
    guint n;
    guint signal_id;
    info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_peer_interface_info.parent_struct, signal_name);
    if (info == NULL)
        return;
    num_params = g_variant_n_children (parameters);
    paramv = g_new0 (GValue, num_params + 1);
    g_value_init (&paramv[0], MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER);
    g_value_set_object (&paramv[0], proxy);
    g_variant_iter_init (&iter, parameters);
    n = 1;
    while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
        _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
        if (arg_info->use_gvariant)
        {
            g_value_init (&paramv[n], G_TYPE_VARIANT);
            g_value_set_variant (&paramv[n], child);
            n++;
        }
        else
            g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
        g_variant_unref (child);
    }
    signal_id = g_signal_lookup (info->signal_name, MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER);
    g_signal_emitv (paramv, signal_id, 0, NULL);
    for (n = 0; n < num_params + 1; n++)
        g_value_unset (&paramv[n]);
    g_free (paramv);
}

static void
mux_org_freedesktop_dbus_peer_proxy_g_properties_changed (GDBusProxy *_proxy,
                                                          GVariant *changed_properties,
                                                          const gchar *const *invalidated_properties)
{
    MuxOrgFreedesktopDBusPeerProxy *proxy = MUX_ORG_FREEDESKTOP_DBUS_PEER_PROXY (_proxy);
    guint n;
    const gchar *key;
    GVariantIter *iter;
    _ExtendedGDBusPropertyInfo *info;
    g_variant_get (changed_properties, "a{sv}", &iter);
    while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
        info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_peer_interface_info.parent_struct, key);
        g_datalist_remove_data (&proxy->priv->qdata, key);
        if (info != NULL)
            g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
    g_variant_iter_free (iter);
    for (n = 0; invalidated_properties[n] != NULL; n++)
    {
        info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_peer_interface_info.parent_struct, invalidated_properties[n]);
        g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
        if (info != NULL)
            g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
mux_org_freedesktop_dbus_peer_proxy_init (MuxOrgFreedesktopDBusPeerProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
    proxy->priv = mux_org_freedesktop_dbus_peer_proxy_get_instance_private (proxy);
#else
    proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, MuxOrgFreedesktopDBusPeerProxyPrivate);
#endif

    g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), mux_org_freedesktop_dbus_peer_interface_info ());
}

static void
mux_org_freedesktop_dbus_peer_proxy_class_init (MuxOrgFreedesktopDBusPeerProxyClass *klass)
{
    GObjectClass *gobject_class;
    GDBusProxyClass *proxy_class;

    gobject_class = G_OBJECT_CLASS (klass);
    gobject_class->finalize     = mux_org_freedesktop_dbus_peer_proxy_finalize;
    gobject_class->get_property = mux_org_freedesktop_dbus_peer_proxy_get_property;
    gobject_class->set_property = mux_org_freedesktop_dbus_peer_proxy_set_property;

    proxy_class = G_DBUS_PROXY_CLASS (klass);
    proxy_class->g_signal = mux_org_freedesktop_dbus_peer_proxy_g_signal;
    proxy_class->g_properties_changed = mux_org_freedesktop_dbus_peer_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
    g_type_class_add_private (klass, sizeof (MuxOrgFreedesktopDBusPeerProxyPrivate));
#endif
}

static void
mux_org_freedesktop_dbus_peer_proxy_iface_init (MuxOrgFreedesktopDBusPeerIface *iface)
{
}

/**
 * mux_org_freedesktop_dbus_peer_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_peer_proxy_new_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_peer_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
mux_org_freedesktop_dbus_peer_proxy_new (
        GDBusConnection     *connection,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GAsyncReadyCallback  callback,
        gpointer             user_data)
{
    g_async_initable_new_async (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Peer", NULL);
}

/**
 * mux_org_freedesktop_dbus_peer_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_peer_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_peer_proxy_new().
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPeerProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusPeer *
mux_org_freedesktop_dbus_peer_proxy_new_finish (
        GAsyncResult        *res,
        GError             **error)
{
    GObject *ret;
    GObject *source_object;
    source_object = g_async_result_get_source_object (res);
    ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
    g_object_unref (source_object);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_PEER (ret);
    else
        return NULL;
}

/**
 * mux_org_freedesktop_dbus_peer_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_peer_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPeerProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusPeer *
mux_org_freedesktop_dbus_peer_proxy_new_sync (
        GDBusConnection     *connection,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GError             **error)
{
    GInitable *ret;
    ret = g_initable_new (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Peer", NULL);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_PEER (ret);
    else
        return NULL;
}


/**
 * mux_org_freedesktop_dbus_peer_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like mux_org_freedesktop_dbus_peer_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_freedesktop_dbus_peer_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See mux_org_freedesktop_dbus_peer_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
mux_org_freedesktop_dbus_peer_proxy_new_for_bus (
        GBusType             bus_type,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GAsyncReadyCallback  callback,
        gpointer             user_data)
{
    g_async_initable_new_async (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Peer", NULL);
}

/**
 * mux_org_freedesktop_dbus_peer_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_freedesktop_dbus_peer_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mux_org_freedesktop_dbus_peer_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPeerProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusPeer *
mux_org_freedesktop_dbus_peer_proxy_new_for_bus_finish (
        GAsyncResult        *res,
        GError             **error)
{
    GObject *ret;
    GObject *source_object;
    source_object = g_async_result_get_source_object (res);
    ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
    g_object_unref (source_object);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_PEER (ret);
    else
        return NULL;
}

/**
 * mux_org_freedesktop_dbus_peer_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like mux_org_freedesktop_dbus_peer_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mux_org_freedesktop_dbus_peer_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPeerProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgFreedesktopDBusPeer *
mux_org_freedesktop_dbus_peer_proxy_new_for_bus_sync (
        GBusType             bus_type,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GError             **error)
{
    GInitable *ret;
    ret = g_initable_new (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Peer", NULL);
    if (ret != NULL)
        return MUX_ORG_FREEDESKTOP_DBUS_PEER (ret);
    else
        return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * MuxOrgFreedesktopDBusPeerSkeleton:
 *
 * The #MuxOrgFreedesktopDBusPeerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MuxOrgFreedesktopDBusPeerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MuxOrgFreedesktopDBusPeerSkeleton.
 */

struct _MuxOrgFreedesktopDBusPeerSkeletonPrivate
{
    GValue *properties;
    GList *changed_properties;
    GSource *changed_properties_idle_source;
    GMainContext *context;
    GMutex lock;
};

static void
_mux_org_freedesktop_dbus_peer_skeleton_handle_method_call (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name,
        const gchar *method_name,
        GVariant *parameters,
        GDBusMethodInvocation *invocation,
        gpointer user_data)
{
    MuxOrgFreedesktopDBusPeerSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (user_data);
    _ExtendedGDBusMethodInfo *info;
    GVariantIter iter;
    GVariant *child;
    GValue *paramv;
    guint num_params;
    guint num_extra;
    guint n;
    guint signal_id;
    GValue return_value = G_VALUE_INIT;
    info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
    g_assert (info != NULL);
    num_params = g_variant_n_children (parameters);
    num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
    n = 0;
    g_value_init (&paramv[n], MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER);
    g_value_set_object (&paramv[n++], skeleton);
    g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
    g_value_set_object (&paramv[n++], invocation);
    if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
        g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
        g_assert_not_reached ();
#endif
    }
    g_variant_iter_init (&iter, parameters);
    while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
        _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
        if (arg_info->use_gvariant)
        {
            g_value_init (&paramv[n], G_TYPE_VARIANT);
            g_value_set_variant (&paramv[n], child);
            n++;
        }
        else
            g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
        g_variant_unref (child);
    }
    signal_id = g_signal_lookup (info->signal_name, MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER);
    g_value_init (&return_value, G_TYPE_BOOLEAN);
    g_signal_emitv (paramv, signal_id, 0, &return_value);
    if (!g_value_get_boolean (&return_value))
        g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
    g_value_unset (&return_value);
    for (n = 0; n < num_params + num_extra; n++)
        g_value_unset (&paramv[n]);
    g_free (paramv);
}

static GVariant *
_mux_org_freedesktop_dbus_peer_skeleton_handle_get_property (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name G_GNUC_UNUSED,
        const gchar *property_name,
        GError **error,
        gpointer user_data)
{
    MuxOrgFreedesktopDBusPeerSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (user_data);
    GValue value = G_VALUE_INIT;
    GParamSpec *pspec;
    _ExtendedGDBusPropertyInfo *info;
    GVariant *ret;
    ret = NULL;
    info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_peer_interface_info.parent_struct, property_name);
    g_assert (info != NULL);
    pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
    if (pspec == NULL)
    {
        g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
    else
    {
        g_value_init (&value, pspec->value_type);
        g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
        ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
        g_value_unset (&value);
    }
    return ret;
}

static gboolean
_mux_org_freedesktop_dbus_peer_skeleton_handle_set_property (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name G_GNUC_UNUSED,
        const gchar *property_name,
        GVariant *variant,
        GError **error,
        gpointer user_data)
{
    MuxOrgFreedesktopDBusPeerSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (user_data);
    GValue value = G_VALUE_INIT;
    GParamSpec *pspec;
    _ExtendedGDBusPropertyInfo *info;
    gboolean ret;
    ret = FALSE;
    info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_freedesktop_dbus_peer_interface_info.parent_struct, property_name);
    g_assert (info != NULL);
    pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
    if (pspec == NULL)
    {
        g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
    else
    {
        if (info->use_gvariant)
            g_value_set_variant (&value, variant);
        else
            g_dbus_gvariant_to_gvalue (variant, &value);
        g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
        g_value_unset (&value);
        ret = TRUE;
    }
    return ret;
}

static const GDBusInterfaceVTable _mux_org_freedesktop_dbus_peer_skeleton_vtable =
        {
                _mux_org_freedesktop_dbus_peer_skeleton_handle_method_call,
                _mux_org_freedesktop_dbus_peer_skeleton_handle_get_property,
                _mux_org_freedesktop_dbus_peer_skeleton_handle_set_property,
                {NULL}
        };

static GDBusInterfaceInfo *
mux_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
    return mux_org_freedesktop_dbus_peer_interface_info ();
}

static GDBusInterfaceVTable *
mux_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
    return (GDBusInterfaceVTable *) &_mux_org_freedesktop_dbus_peer_skeleton_vtable;
}

static GVariant *
mux_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
    MuxOrgFreedesktopDBusPeerSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (_skeleton);

    GVariantBuilder builder;
    guint n;
    g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
    if (_mux_org_freedesktop_dbus_peer_interface_info.parent_struct.properties == NULL)
        goto out;
    for (n = 0; _mux_org_freedesktop_dbus_peer_interface_info.parent_struct.properties[n] != NULL; n++)
    {
        GDBusPropertyInfo *info = _mux_org_freedesktop_dbus_peer_interface_info.parent_struct.properties[n];
        if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
            GVariant *value;
            value = _mux_org_freedesktop_dbus_peer_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Peer", info->name, NULL, skeleton);
            if (value != NULL)
            {
                g_variant_take_ref (value);
                g_variant_builder_add (&builder, "{sv}", info->name, value);
                g_variant_unref (value);
            }
        }
    }
    out:
    return g_variant_builder_end (&builder);
}

static void
mux_org_freedesktop_dbus_peer_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void mux_org_freedesktop_dbus_peer_skeleton_iface_init (MuxOrgFreedesktopDBusPeerIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusPeerSkeleton, mux_org_freedesktop_dbus_peer_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
        G_ADD_PRIVATE (MuxOrgFreedesktopDBusPeerSkeleton)
G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER, mux_org_freedesktop_dbus_peer_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MuxOrgFreedesktopDBusPeerSkeleton, mux_org_freedesktop_dbus_peer_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER, mux_org_freedesktop_dbus_peer_skeleton_iface_init));

#endif
static void
mux_org_freedesktop_dbus_peer_skeleton_finalize (GObject *object)
{
    MuxOrgFreedesktopDBusPeerSkeleton *skeleton = MUX_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (object);
    g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
    if (skeleton->priv->changed_properties_idle_source != NULL)
        g_source_destroy (skeleton->priv->changed_properties_idle_source);
    g_main_context_unref (skeleton->priv->context);
    g_mutex_clear (&skeleton->priv->lock);
    G_OBJECT_CLASS (mux_org_freedesktop_dbus_peer_skeleton_parent_class)->finalize (object);
}

static void
mux_org_freedesktop_dbus_peer_skeleton_init (MuxOrgFreedesktopDBusPeerSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
    skeleton->priv = mux_org_freedesktop_dbus_peer_skeleton_get_instance_private (skeleton);
#else
    skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER_SKELETON, MuxOrgFreedesktopDBusPeerSkeletonPrivate);
#endif

    g_mutex_init (&skeleton->priv->lock);
    skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
mux_org_freedesktop_dbus_peer_skeleton_class_init (MuxOrgFreedesktopDBusPeerSkeletonClass *klass)
{
    GObjectClass *gobject_class;
    GDBusInterfaceSkeletonClass *skeleton_class;

    gobject_class = G_OBJECT_CLASS (klass);
    gobject_class->finalize = mux_org_freedesktop_dbus_peer_skeleton_finalize;

    skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
    skeleton_class->get_info = mux_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_info;
    skeleton_class->get_properties = mux_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_properties;
    skeleton_class->flush = mux_org_freedesktop_dbus_peer_skeleton_dbus_interface_flush;
    skeleton_class->get_vtable = mux_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
    g_type_class_add_private (klass, sizeof (MuxOrgFreedesktopDBusPeerSkeletonPrivate));
#endif
}

static void
mux_org_freedesktop_dbus_peer_skeleton_iface_init (MuxOrgFreedesktopDBusPeerIface *iface)
{
}

/**
 * mux_org_freedesktop_dbus_peer_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>.
 *
 * Returns: (transfer full) (type MuxOrgFreedesktopDBusPeerSkeleton): The skeleton object.
 */
MuxOrgFreedesktopDBusPeer *
mux_org_freedesktop_dbus_peer_skeleton_new (void)
{
    return MUX_ORG_FREEDESKTOP_DBUS_PEER (g_object_new (MUX_TYPE_ORG_FREEDESKTOP_DBUS_PEER_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.RDPMux.RDPMux
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:MuxOrgRDPMuxRDPMux
 * @title: MuxOrgRDPMuxRDPMux
 * @short_description: Generated C code for the org.RDPMux.RDPMux D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-RDPMux-RDPMux.top_of_page">org.RDPMux.RDPMux</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.RDPMux.RDPMux ---- */

static const _ExtendedGDBusArgInfo _mux_org_rdpmux_rdpmux_method_info_register_IN_ARG_id =
        {
                {
                        -1,
                        (gchar *) "id",
                        (gchar *) "i",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo _mux_org_rdpmux_rdpmux_method_info_register_IN_ARG_version =
        {
                {
                        -1,
                        (gchar *) "version",
                        (gchar *) "i",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_rdpmux_rdpmux_method_info_register_IN_ARG_pointers[] =
        {
                &_mux_org_rdpmux_rdpmux_method_info_register_IN_ARG_id,
                &_mux_org_rdpmux_rdpmux_method_info_register_IN_ARG_version,
                NULL
        };

static const _ExtendedGDBusArgInfo _mux_org_rdpmux_rdpmux_method_info_register_OUT_ARG_socket_path =
        {
                {
                        -1,
                        (gchar *) "socket_path",
                        (gchar *) "s",
                        NULL
                },
                FALSE
        };

static const _ExtendedGDBusArgInfo * const _mux_org_rdpmux_rdpmux_method_info_register_OUT_ARG_pointers[] =
        {
                &_mux_org_rdpmux_rdpmux_method_info_register_OUT_ARG_socket_path,
                NULL
        };

static const _ExtendedGDBusMethodInfo _mux_org_rdpmux_rdpmux_method_info_register =
        {
                {
                        -1,
                        (gchar *) "Register",
                        (GDBusArgInfo **) &_mux_org_rdpmux_rdpmux_method_info_register_IN_ARG_pointers,
                        (GDBusArgInfo **) &_mux_org_rdpmux_rdpmux_method_info_register_OUT_ARG_pointers,
                        NULL
                },
                "handle-register",
                FALSE
        };

static const _ExtendedGDBusMethodInfo * const _mux_org_rdpmux_rdpmux_method_info_pointers[] =
        {
                &_mux_org_rdpmux_rdpmux_method_info_register,
                NULL
        };

static const _ExtendedGDBusPropertyInfo _mux_org_rdpmux_rdpmux_property_info_supported_protocol_versions =
        {
                {
                        -1,
                        (gchar *) "SupportedProtocolVersions",
                        (gchar *) "ai",
                        G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
                        NULL
                },
                "supported-protocol-versions",
                FALSE
        };

static const _ExtendedGDBusPropertyInfo * const _mux_org_rdpmux_rdpmux_property_info_pointers[] =
        {
                &_mux_org_rdpmux_rdpmux_property_info_supported_protocol_versions,
                NULL
        };

static const _ExtendedGDBusInterfaceInfo _mux_org_rdpmux_rdpmux_interface_info =
        {
                {
                        -1,
                        (gchar *) "org.RDPMux.RDPMux",
                        (GDBusMethodInfo **) &_mux_org_rdpmux_rdpmux_method_info_pointers,
                        NULL,
                        (GDBusPropertyInfo **) &_mux_org_rdpmux_rdpmux_property_info_pointers,
                        NULL
                },
                "org-rdpmux-rdpmux",
        };


/**
 * mux_org_rdpmux_rdpmux_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-RDPMux-RDPMux.top_of_page">org.RDPMux.RDPMux</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
mux_org_rdpmux_rdpmux_interface_info (void)
{
    return (GDBusInterfaceInfo *) &_mux_org_rdpmux_rdpmux_interface_info.parent_struct;
}

/**
 * mux_org_rdpmux_rdpmux_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #MuxOrgRDPMuxRDPMux interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
mux_org_rdpmux_rdpmux_override_properties (GObjectClass *klass, guint property_id_begin)
{
    g_object_class_override_property (klass, property_id_begin++, "supported-protocol-versions");
    return property_id_begin - 1;
}



/**
 * MuxOrgRDPMuxRDPMux:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-RDPMux-RDPMux.top_of_page">org.RDPMux.RDPMux</link>.
 */

/**
 * MuxOrgRDPMuxRDPMuxIface:
 * @parent_iface: The parent interface.
 * @handle_register: Handler for the #MuxOrgRDPMuxRDPMux::handle-register signal.
 * @get_supported_protocol_versions: Getter for the #MuxOrgRDPMuxRDPMux:supported-protocol-versions property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-RDPMux-RDPMux.top_of_page">org.RDPMux.RDPMux</link>.
 */

typedef MuxOrgRDPMuxRDPMuxIface MuxOrgRDPMuxRDPMuxInterface;
G_DEFINE_INTERFACE (MuxOrgRDPMuxRDPMux, mux_org_rdpmux_rdpmux, G_TYPE_OBJECT);

static void
mux_org_rdpmux_rdpmux_default_init (MuxOrgRDPMuxRDPMuxIface *iface)
{
    /* GObject signals for incoming D-Bus method calls: */
    /**
     * MuxOrgRDPMuxRDPMux::handle-register:
     * @object: A #MuxOrgRDPMuxRDPMux.
     * @invocation: A #GDBusMethodInvocation.
     * @arg_id: Argument passed by remote caller.
     * @arg_version: Argument passed by remote caller.
     *
     * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-RDPMux-RDPMux.Register">Register()</link> D-Bus method.
     *
     * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mux_org_rdpmux_rdpmux_complete_register() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
     *
     * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
     */
    g_signal_new ("handle-register",
                  G_TYPE_FROM_INTERFACE (iface),
                  G_SIGNAL_RUN_LAST,
                  G_STRUCT_OFFSET (MuxOrgRDPMuxRDPMuxIface, handle_register),
                  g_signal_accumulator_true_handled,
                  NULL,
                  g_cclosure_marshal_generic,
                  G_TYPE_BOOLEAN,
                  3,
                  G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT, G_TYPE_INT);

    /* GObject properties for D-Bus properties: */
    /**
     * MuxOrgRDPMuxRDPMux:supported-protocol-versions:
     *
     * Represents the D-Bus property <link linkend="gdbus-property-org-RDPMux-RDPMux.SupportedProtocolVersions">"SupportedProtocolVersions"</link>.
     *
     * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
     */
    g_object_interface_install_property (iface,
                                         g_param_spec_variant ("supported-protocol-versions", "SupportedProtocolVersions", "SupportedProtocolVersions", G_VARIANT_TYPE ("ai"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * mux_org_rdpmux_rdpmux_get_supported_protocol_versions: (skip)
 * @object: A #MuxOrgRDPMuxRDPMux.
 *
 * Gets the value of the <link linkend="gdbus-property-org-RDPMux-RDPMux.SupportedProtocolVersions">"SupportedProtocolVersions"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use mux_org_rdpmux_rdpmux_dup_supported_protocol_versions() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
mux_org_rdpmux_rdpmux_get_supported_protocol_versions (MuxOrgRDPMuxRDPMux *object)
{
    return MUX_ORG_RDPMUX_RDPMUX_GET_IFACE (object)->get_supported_protocol_versions (object);
}

/**
 * mux_org_rdpmux_rdpmux_dup_supported_protocol_versions: (skip)
 * @object: A #MuxOrgRDPMuxRDPMux.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-RDPMux-RDPMux.SupportedProtocolVersions">"SupportedProtocolVersions"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
mux_org_rdpmux_rdpmux_dup_supported_protocol_versions (MuxOrgRDPMuxRDPMux *object)
{
    GVariant *value;
    g_object_get (G_OBJECT (object), "supported-protocol-versions", &value, NULL);
    return value;
}

/**
 * mux_org_rdpmux_rdpmux_set_supported_protocol_versions: (skip)
 * @object: A #MuxOrgRDPMuxRDPMux.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-RDPMux-RDPMux.SupportedProtocolVersions">"SupportedProtocolVersions"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
mux_org_rdpmux_rdpmux_set_supported_protocol_versions (MuxOrgRDPMuxRDPMux *object, GVariant *value)
{
    g_object_set (G_OBJECT (object), "supported-protocol-versions", value, NULL);
}

/**
 * mux_org_rdpmux_rdpmux_call_register:
 * @proxy: A #MuxOrgRDPMuxRDPMuxProxy.
 * @arg_id: Argument to pass with the method invocation.
 * @arg_version: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-RDPMux-RDPMux.Register">Register()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_rdpmux_rdpmux_call_register_finish() to get the result of the operation.
 *
 * See mux_org_rdpmux_rdpmux_call_register_sync() for the synchronous, blocking version of this method.
 */
void
        mux_org_rdpmux_rdpmux_call_register (
        MuxOrgRDPMuxRDPMux *proxy,
        gint arg_id,
gint arg_version,
        GCancellable *cancellable,
GAsyncReadyCallback callback,
        gpointer user_data)
{
g_dbus_proxy_call (G_DBUS_PROXY (proxy),
"Register",
g_variant_new ("(ii)",
arg_id,
arg_version),
G_DBUS_CALL_FLAGS_NONE,
-1,
cancellable,
callback,
user_data);
}

/**
 * mux_org_rdpmux_rdpmux_call_register_finish:
 * @proxy: A #MuxOrgRDPMuxRDPMuxProxy.
 * @out_socket_path: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_rdpmux_rdpmux_call_register().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mux_org_rdpmux_rdpmux_call_register().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mux_org_rdpmux_rdpmux_call_register_finish (
        MuxOrgRDPMuxRDPMux *proxy,
        gchar **out_socket_path,
        GAsyncResult *res,
        GError **error)
{
    GVariant *_ret;
    _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
    if (_ret == NULL)
        goto _out;
    g_variant_get (_ret,
                   "(s)",
                   out_socket_path);
    g_variant_unref (_ret);
    _out:
    return _ret != NULL;
}

/**
 * mux_org_rdpmux_rdpmux_call_register_sync:
 * @proxy: A #MuxOrgRDPMuxRDPMuxProxy.
 * @arg_id: Argument to pass with the method invocation.
 * @arg_version: Argument to pass with the method invocation.
 * @out_socket_path: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-RDPMux-RDPMux.Register">Register()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mux_org_rdpmux_rdpmux_call_register() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
        mux_org_rdpmux_rdpmux_call_register_sync (
        MuxOrgRDPMuxRDPMux *proxy,
        gint arg_id,
gint arg_version,
        gchar **out_socket_path,
GCancellable *cancellable,
        GError **error)
{
GVariant *_ret;
_ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
                               "Register",
                               g_variant_new ("(ii)",
                                              arg_id,
                                              arg_version),
                               G_DBUS_CALL_FLAGS_NONE,
                               -1,
                               cancellable,
                               error);
if (_ret == NULL)
goto _out;
g_variant_get (_ret,
"(s)",
out_socket_path);
g_variant_unref (_ret);
_out:
return _ret != NULL;
}

/**
 * mux_org_rdpmux_rdpmux_complete_register:
 * @object: A #MuxOrgRDPMuxRDPMux.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @socket_path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-RDPMux-RDPMux.Register">Register()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
        mux_org_rdpmux_rdpmux_complete_register (
        MuxOrgRDPMuxRDPMux *object,
        GDBusMethodInvocation *invocation,
        const gchar *socket_path)
{
g_dbus_method_invocation_return_value (invocation,
        g_variant_new ("(s)",
                       socket_path));
}

/* ------------------------------------------------------------------------ */

/**
 * MuxOrgRDPMuxRDPMuxProxy:
 *
 * The #MuxOrgRDPMuxRDPMuxProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MuxOrgRDPMuxRDPMuxProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MuxOrgRDPMuxRDPMuxProxy.
 */

struct _MuxOrgRDPMuxRDPMuxProxyPrivate
{
    GData *qdata;
};

static void mux_org_rdpmux_rdpmux_proxy_iface_init (MuxOrgRDPMuxRDPMuxIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MuxOrgRDPMuxRDPMuxProxy, mux_org_rdpmux_rdpmux_proxy, G_TYPE_DBUS_PROXY,
        G_ADD_PRIVATE (MuxOrgRDPMuxRDPMuxProxy)
G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_RDPMUX_RDPMUX, mux_org_rdpmux_rdpmux_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MuxOrgRDPMuxRDPMuxProxy, mux_org_rdpmux_rdpmux_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_RDPMUX_RDPMUX, mux_org_rdpmux_rdpmux_proxy_iface_init));

#endif
static void
mux_org_rdpmux_rdpmux_proxy_finalize (GObject *object)
{
    MuxOrgRDPMuxRDPMuxProxy *proxy = MUX_ORG_RDPMUX_RDPMUX_PROXY (object);
    g_datalist_clear (&proxy->priv->qdata);
    G_OBJECT_CLASS (mux_org_rdpmux_rdpmux_proxy_parent_class)->finalize (object);
}

static void
mux_org_rdpmux_rdpmux_proxy_get_property (GObject      *object,
                                          guint         prop_id,
                                          GValue       *value,
                                          GParamSpec   *pspec G_GNUC_UNUSED)
{
    const _ExtendedGDBusPropertyInfo *info;
    GVariant *variant;
    g_assert (prop_id != 0 && prop_id - 1 < 1);
    info = _mux_org_rdpmux_rdpmux_property_info_pointers[prop_id - 1];
    variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
    if (info->use_gvariant)
    {
        g_value_set_variant (value, variant);
    }
    else
    {
        if (variant != NULL)
            g_dbus_gvariant_to_gvalue (variant, value);
    }
    if (variant != NULL)
        g_variant_unref (variant);
}

static void
mux_org_rdpmux_rdpmux_proxy_set_property_cb (GDBusProxy *proxy,
                                             GAsyncResult *res,
                                             gpointer      user_data)
{
    const _ExtendedGDBusPropertyInfo *info = user_data;
    GError *error;
    GVariant *_ret;
    error = NULL;
    _ret = g_dbus_proxy_call_finish (proxy, res, &error);
    if (!_ret)
    {
        g_warning ("Error setting property '%s' on interface org.RDPMux.RDPMux: %s (%s, %d)",
                   info->parent_struct.name,
                   error->message, g_quark_to_string (error->domain), error->code);
        g_error_free (error);
    }
    else
    {
        g_variant_unref (_ret);
    }
}

static void
mux_org_rdpmux_rdpmux_proxy_set_property (GObject      *object,
                                          guint         prop_id,
                                          const GValue *value,
                                          GParamSpec   *pspec G_GNUC_UNUSED)
{
    const _ExtendedGDBusPropertyInfo *info;
    GVariant *variant;
    g_assert (prop_id != 0 && prop_id - 1 < 1);
    info = _mux_org_rdpmux_rdpmux_property_info_pointers[prop_id - 1];
    variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
    g_dbus_proxy_call (G_DBUS_PROXY (object),
                       "org.freedesktop.DBus.Properties.Set",
                       g_variant_new ("(ssv)", "org.RDPMux.RDPMux", info->parent_struct.name, variant),
                       G_DBUS_CALL_FLAGS_NONE,
                       -1,
                       NULL, (GAsyncReadyCallback) mux_org_rdpmux_rdpmux_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
    g_variant_unref (variant);
}

static void
mux_org_rdpmux_rdpmux_proxy_g_signal (GDBusProxy *proxy,
                                      const gchar *sender_name G_GNUC_UNUSED,
                                      const gchar *signal_name,
                                      GVariant *parameters)
{
    _ExtendedGDBusSignalInfo *info;
    GVariantIter iter;
    GVariant *child;
    GValue *paramv;
    guint num_params;
    guint n;
    guint signal_id;
    info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_mux_org_rdpmux_rdpmux_interface_info.parent_struct, signal_name);
    if (info == NULL)
        return;
    num_params = g_variant_n_children (parameters);
    paramv = g_new0 (GValue, num_params + 1);
    g_value_init (&paramv[0], MUX_TYPE_ORG_RDPMUX_RDPMUX);
    g_value_set_object (&paramv[0], proxy);
    g_variant_iter_init (&iter, parameters);
    n = 1;
    while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
        _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
        if (arg_info->use_gvariant)
        {
            g_value_init (&paramv[n], G_TYPE_VARIANT);
            g_value_set_variant (&paramv[n], child);
            n++;
        }
        else
            g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
        g_variant_unref (child);
    }
    signal_id = g_signal_lookup (info->signal_name, MUX_TYPE_ORG_RDPMUX_RDPMUX);
    g_signal_emitv (paramv, signal_id, 0, NULL);
    for (n = 0; n < num_params + 1; n++)
        g_value_unset (&paramv[n]);
    g_free (paramv);
}

static void
mux_org_rdpmux_rdpmux_proxy_g_properties_changed (GDBusProxy *_proxy,
                                                  GVariant *changed_properties,
                                                  const gchar *const *invalidated_properties)
{
    MuxOrgRDPMuxRDPMuxProxy *proxy = MUX_ORG_RDPMUX_RDPMUX_PROXY (_proxy);
    guint n;
    const gchar *key;
    GVariantIter *iter;
    _ExtendedGDBusPropertyInfo *info;
    g_variant_get (changed_properties, "a{sv}", &iter);
    while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
        info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_rdpmux_rdpmux_interface_info.parent_struct, key);
        g_datalist_remove_data (&proxy->priv->qdata, key);
        if (info != NULL)
            g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
    g_variant_iter_free (iter);
    for (n = 0; invalidated_properties[n] != NULL; n++)
    {
        info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_rdpmux_rdpmux_interface_info.parent_struct, invalidated_properties[n]);
        g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
        if (info != NULL)
            g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static GVariant *
mux_org_rdpmux_rdpmux_proxy_get_supported_protocol_versions (MuxOrgRDPMuxRDPMux *object)
{
    MuxOrgRDPMuxRDPMuxProxy *proxy = MUX_ORG_RDPMUX_RDPMUX_PROXY (object);
    GVariant *variant;
    GVariant *value = NULL;
    variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SupportedProtocolVersions");
    value = variant;
    if (variant != NULL)
        g_variant_unref (variant);
    return value;
}

static void
mux_org_rdpmux_rdpmux_proxy_init (MuxOrgRDPMuxRDPMuxProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
    proxy->priv = mux_org_rdpmux_rdpmux_proxy_get_instance_private (proxy);
#else
    proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, MUX_TYPE_ORG_RDPMUX_RDPMUX_PROXY, MuxOrgRDPMuxRDPMuxProxyPrivate);
#endif

    g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), mux_org_rdpmux_rdpmux_interface_info ());
}

static void
mux_org_rdpmux_rdpmux_proxy_class_init (MuxOrgRDPMuxRDPMuxProxyClass *klass)
{
    GObjectClass *gobject_class;
    GDBusProxyClass *proxy_class;

    gobject_class = G_OBJECT_CLASS (klass);
    gobject_class->finalize     = mux_org_rdpmux_rdpmux_proxy_finalize;
    gobject_class->get_property = mux_org_rdpmux_rdpmux_proxy_get_property;
    gobject_class->set_property = mux_org_rdpmux_rdpmux_proxy_set_property;

    proxy_class = G_DBUS_PROXY_CLASS (klass);
    proxy_class->g_signal = mux_org_rdpmux_rdpmux_proxy_g_signal;
    proxy_class->g_properties_changed = mux_org_rdpmux_rdpmux_proxy_g_properties_changed;

    mux_org_rdpmux_rdpmux_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
    g_type_class_add_private (klass, sizeof (MuxOrgRDPMuxRDPMuxProxyPrivate));
#endif
}

static void
mux_org_rdpmux_rdpmux_proxy_iface_init (MuxOrgRDPMuxRDPMuxIface *iface)
{
    iface->get_supported_protocol_versions = mux_org_rdpmux_rdpmux_proxy_get_supported_protocol_versions;
}

/**
 * mux_org_rdpmux_rdpmux_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-RDPMux-RDPMux.top_of_page">org.RDPMux.RDPMux</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_rdpmux_rdpmux_proxy_new_finish() to get the result of the operation.
 *
 * See mux_org_rdpmux_rdpmux_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
mux_org_rdpmux_rdpmux_proxy_new (
        GDBusConnection     *connection,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GAsyncReadyCallback  callback,
        gpointer             user_data)
{
    g_async_initable_new_async (MUX_TYPE_ORG_RDPMUX_RDPMUX_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.RDPMux.RDPMux", NULL);
}

/**
 * mux_org_rdpmux_rdpmux_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_rdpmux_rdpmux_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mux_org_rdpmux_rdpmux_proxy_new().
 *
 * Returns: (transfer full) (type MuxOrgRDPMuxRDPMuxProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgRDPMuxRDPMux *
mux_org_rdpmux_rdpmux_proxy_new_finish (
        GAsyncResult        *res,
        GError             **error)
{
    GObject *ret;
    GObject *source_object;
    source_object = g_async_result_get_source_object (res);
    ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
    g_object_unref (source_object);
    if (ret != NULL)
        return MUX_ORG_RDPMUX_RDPMUX (ret);
    else
        return NULL;
}

/**
 * mux_org_rdpmux_rdpmux_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-RDPMux-RDPMux.top_of_page">org.RDPMux.RDPMux</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mux_org_rdpmux_rdpmux_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MuxOrgRDPMuxRDPMuxProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgRDPMuxRDPMux *
mux_org_rdpmux_rdpmux_proxy_new_sync (
        GDBusConnection     *connection,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GError             **error)
{
    GInitable *ret;
    ret = g_initable_new (MUX_TYPE_ORG_RDPMUX_RDPMUX_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.RDPMux.RDPMux", NULL);
    if (ret != NULL)
        return MUX_ORG_RDPMUX_RDPMUX (ret);
    else
        return NULL;
}


/**
 * mux_org_rdpmux_rdpmux_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like mux_org_rdpmux_rdpmux_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mux_org_rdpmux_rdpmux_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See mux_org_rdpmux_rdpmux_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
mux_org_rdpmux_rdpmux_proxy_new_for_bus (
        GBusType             bus_type,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GAsyncReadyCallback  callback,
        gpointer             user_data)
{
    g_async_initable_new_async (MUX_TYPE_ORG_RDPMUX_RDPMUX_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.RDPMux.RDPMux", NULL);
}

/**
 * mux_org_rdpmux_rdpmux_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mux_org_rdpmux_rdpmux_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mux_org_rdpmux_rdpmux_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type MuxOrgRDPMuxRDPMuxProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgRDPMuxRDPMux *
mux_org_rdpmux_rdpmux_proxy_new_for_bus_finish (
        GAsyncResult        *res,
        GError             **error)
{
    GObject *ret;
    GObject *source_object;
    source_object = g_async_result_get_source_object (res);
    ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
    g_object_unref (source_object);
    if (ret != NULL)
        return MUX_ORG_RDPMUX_RDPMUX (ret);
    else
        return NULL;
}

/**
 * mux_org_rdpmux_rdpmux_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like mux_org_rdpmux_rdpmux_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mux_org_rdpmux_rdpmux_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MuxOrgRDPMuxRDPMuxProxy): The constructed proxy object or %NULL if @error is set.
 */
MuxOrgRDPMuxRDPMux *
mux_org_rdpmux_rdpmux_proxy_new_for_bus_sync (
        GBusType             bus_type,
        GDBusProxyFlags      flags,
        const gchar         *name,
        const gchar         *object_path,
        GCancellable        *cancellable,
        GError             **error)
{
    GInitable *ret;
    ret = g_initable_new (MUX_TYPE_ORG_RDPMUX_RDPMUX_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.RDPMux.RDPMux", NULL);
    if (ret != NULL)
        return MUX_ORG_RDPMUX_RDPMUX (ret);
    else
        return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * MuxOrgRDPMuxRDPMuxSkeleton:
 *
 * The #MuxOrgRDPMuxRDPMuxSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MuxOrgRDPMuxRDPMuxSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MuxOrgRDPMuxRDPMuxSkeleton.
 */

struct _MuxOrgRDPMuxRDPMuxSkeletonPrivate
{
    GValue *properties;
    GList *changed_properties;
    GSource *changed_properties_idle_source;
    GMainContext *context;
    GMutex lock;
};

static void
_mux_org_rdpmux_rdpmux_skeleton_handle_method_call (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name,
        const gchar *method_name,
        GVariant *parameters,
        GDBusMethodInvocation *invocation,
        gpointer user_data)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (user_data);
    _ExtendedGDBusMethodInfo *info;
    GVariantIter iter;
    GVariant *child;
    GValue *paramv;
    guint num_params;
    guint num_extra;
    guint n;
    guint signal_id;
    GValue return_value = G_VALUE_INIT;
    info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
    g_assert (info != NULL);
    num_params = g_variant_n_children (parameters);
    num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
    n = 0;
    g_value_init (&paramv[n], MUX_TYPE_ORG_RDPMUX_RDPMUX);
    g_value_set_object (&paramv[n++], skeleton);
    g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
    g_value_set_object (&paramv[n++], invocation);
    if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
        g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
        g_assert_not_reached ();
#endif
    }
    g_variant_iter_init (&iter, parameters);
    while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
        _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
        if (arg_info->use_gvariant)
        {
            g_value_init (&paramv[n], G_TYPE_VARIANT);
            g_value_set_variant (&paramv[n], child);
            n++;
        }
        else
            g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
        g_variant_unref (child);
    }
    signal_id = g_signal_lookup (info->signal_name, MUX_TYPE_ORG_RDPMUX_RDPMUX);
    g_value_init (&return_value, G_TYPE_BOOLEAN);
    g_signal_emitv (paramv, signal_id, 0, &return_value);
    if (!g_value_get_boolean (&return_value))
        g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
    g_value_unset (&return_value);
    for (n = 0; n < num_params + num_extra; n++)
        g_value_unset (&paramv[n]);
    g_free (paramv);
}

static GVariant *
_mux_org_rdpmux_rdpmux_skeleton_handle_get_property (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name G_GNUC_UNUSED,
        const gchar *property_name,
        GError **error,
        gpointer user_data)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (user_data);
    GValue value = G_VALUE_INIT;
    GParamSpec *pspec;
    _ExtendedGDBusPropertyInfo *info;
    GVariant *ret;
    ret = NULL;
    info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_rdpmux_rdpmux_interface_info.parent_struct, property_name);
    g_assert (info != NULL);
    pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
    if (pspec == NULL)
    {
        g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
    else
    {
        g_value_init (&value, pspec->value_type);
        g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
        ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
        g_value_unset (&value);
    }
    return ret;
}

static gboolean
_mux_org_rdpmux_rdpmux_skeleton_handle_set_property (
        GDBusConnection *connection G_GNUC_UNUSED,
        const gchar *sender G_GNUC_UNUSED,
        const gchar *object_path G_GNUC_UNUSED,
        const gchar *interface_name G_GNUC_UNUSED,
        const gchar *property_name,
        GVariant *variant,
        GError **error,
        gpointer user_data)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (user_data);
    GValue value = G_VALUE_INIT;
    GParamSpec *pspec;
    _ExtendedGDBusPropertyInfo *info;
    gboolean ret;
    ret = FALSE;
    info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mux_org_rdpmux_rdpmux_interface_info.parent_struct, property_name);
    g_assert (info != NULL);
    pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
    if (pspec == NULL)
    {
        g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
    else
    {
        if (info->use_gvariant)
            g_value_set_variant (&value, variant);
        else
            g_dbus_gvariant_to_gvalue (variant, &value);
        g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
        g_value_unset (&value);
        ret = TRUE;
    }
    return ret;
}

static const GDBusInterfaceVTable _mux_org_rdpmux_rdpmux_skeleton_vtable =
        {
                _mux_org_rdpmux_rdpmux_skeleton_handle_method_call,
                _mux_org_rdpmux_rdpmux_skeleton_handle_get_property,
                _mux_org_rdpmux_rdpmux_skeleton_handle_set_property,
                {NULL}
        };

static GDBusInterfaceInfo *
mux_org_rdpmux_rdpmux_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
    return mux_org_rdpmux_rdpmux_interface_info ();
}

static GDBusInterfaceVTable *
mux_org_rdpmux_rdpmux_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
    return (GDBusInterfaceVTable *) &_mux_org_rdpmux_rdpmux_skeleton_vtable;
}

static GVariant *
mux_org_rdpmux_rdpmux_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (_skeleton);

    GVariantBuilder builder;
    guint n;
    g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
    if (_mux_org_rdpmux_rdpmux_interface_info.parent_struct.properties == NULL)
        goto out;
    for (n = 0; _mux_org_rdpmux_rdpmux_interface_info.parent_struct.properties[n] != NULL; n++)
    {
        GDBusPropertyInfo *info = _mux_org_rdpmux_rdpmux_interface_info.parent_struct.properties[n];
        if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
            GVariant *value;
            value = _mux_org_rdpmux_rdpmux_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.RDPMux.RDPMux", info->name, NULL, skeleton);
            if (value != NULL)
            {
                g_variant_take_ref (value);
                g_variant_builder_add (&builder, "{sv}", info->name, value);
                g_variant_unref (value);
            }
        }
    }
    out:
    return g_variant_builder_end (&builder);
}

static gboolean _mux_org_rdpmux_rdpmux_emit_changed (gpointer user_data);

static void
mux_org_rdpmux_rdpmux_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (_skeleton);
    gboolean emit_changed = FALSE;

    g_mutex_lock (&skeleton->priv->lock);
    if (skeleton->priv->changed_properties_idle_source != NULL)
    {
        g_source_destroy (skeleton->priv->changed_properties_idle_source);
        skeleton->priv->changed_properties_idle_source = NULL;
        emit_changed = TRUE;
    }
    g_mutex_unlock (&skeleton->priv->lock);

    if (emit_changed)
        _mux_org_rdpmux_rdpmux_emit_changed (skeleton);
}

static void mux_org_rdpmux_rdpmux_skeleton_iface_init (MuxOrgRDPMuxRDPMuxIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MuxOrgRDPMuxRDPMuxSkeleton, mux_org_rdpmux_rdpmux_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
        G_ADD_PRIVATE (MuxOrgRDPMuxRDPMuxSkeleton)
G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_RDPMUX_RDPMUX, mux_org_rdpmux_rdpmux_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MuxOrgRDPMuxRDPMuxSkeleton, mux_org_rdpmux_rdpmux_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (MUX_TYPE_ORG_RDPMUX_RDPMUX, mux_org_rdpmux_rdpmux_skeleton_iface_init));

#endif
static void
mux_org_rdpmux_rdpmux_skeleton_finalize (GObject *object)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (object);
    guint n;
    for (n = 0; n < 1; n++)
        g_value_unset (&skeleton->priv->properties[n]);
    g_free (skeleton->priv->properties);
    g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
    if (skeleton->priv->changed_properties_idle_source != NULL)
        g_source_destroy (skeleton->priv->changed_properties_idle_source);
    g_main_context_unref (skeleton->priv->context);
    g_mutex_clear (&skeleton->priv->lock);
    G_OBJECT_CLASS (mux_org_rdpmux_rdpmux_skeleton_parent_class)->finalize (object);
}

static void
mux_org_rdpmux_rdpmux_skeleton_get_property (GObject      *object,
                                             guint         prop_id,
                                             GValue       *value,
                                             GParamSpec   *pspec G_GNUC_UNUSED)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (object);
    g_assert (prop_id != 0 && prop_id - 1 < 1);
    g_mutex_lock (&skeleton->priv->lock);
    g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
    g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
        _mux_org_rdpmux_rdpmux_emit_changed (gpointer user_data)
{
MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (user_data);
GList *l;
GVariantBuilder builder;
GVariantBuilder invalidated_builder;
guint num_changes;

g_mutex_lock (&skeleton->priv->lock);
g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
{
ChangedProperty *cp = l->data;
GVariant *variant;
const GValue *cur_value;

cur_value = &skeleton->priv->properties[cp->prop_id - 1];
if (!_g_value_equal (cur_value, &cp->orig_value))
{
variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
g_variant_unref (variant);
num_changes++;
}
}
if (num_changes > 0)
{
GList *connections, *ll;
GVariant *signal_variant;
signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.RDPMux.RDPMux",
                                                    &builder, &invalidated_builder));
connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
for (ll = connections; ll != NULL; ll = ll->next)
{
GDBusConnection *connection = ll->data;

g_dbus_connection_emit_signal (connection,
NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
"org.freedesktop.DBus.Properties",
"PropertiesChanged",
signal_variant,
NULL);
}
g_variant_unref (signal_variant);
g_list_free_full (connections, g_object_unref);
}
else
{
g_variant_builder_clear (&builder);
g_variant_builder_clear (&invalidated_builder);
}
g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
skeleton->priv->changed_properties = NULL;
skeleton->priv->changed_properties_idle_source = NULL;
g_mutex_unlock (&skeleton->priv->lock);
return FALSE;
}

static void
        _mux_org_rdpmux_rdpmux_schedule_emit_changed (MuxOrgRDPMuxRDPMuxSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
ChangedProperty *cp;
GList *l;
cp = NULL;
for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
{
ChangedProperty *i_cp = l->data;
if (i_cp->info == info)
{
cp = i_cp;
break;
}
}
if (cp == NULL)
{
cp = g_new0 (ChangedProperty, 1);
cp->prop_id = prop_id;
cp->info = info;
skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
g_value_copy (orig_value, &cp->orig_value);
}
}

static void
mux_org_rdpmux_rdpmux_skeleton_notify (GObject      *object,
                                       GParamSpec *pspec G_GNUC_UNUSED)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (object);
    g_mutex_lock (&skeleton->priv->lock);
    if (skeleton->priv->changed_properties != NULL &&
        skeleton->priv->changed_properties_idle_source == NULL)
    {
        skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
        g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
        g_source_set_callback (skeleton->priv->changed_properties_idle_source, _mux_org_rdpmux_rdpmux_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
        g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _mux_org_rdpmux_rdpmux_emit_changed");
        g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
        g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
    g_mutex_unlock (&skeleton->priv->lock);
}

static void
mux_org_rdpmux_rdpmux_skeleton_set_property (GObject      *object,
                                             guint         prop_id,
                                             const GValue *value,
                                             GParamSpec   *pspec)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (object);
    g_assert (prop_id != 0 && prop_id - 1 < 1);
    g_mutex_lock (&skeleton->priv->lock);
    g_object_freeze_notify (object);
    if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
        if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
            _mux_org_rdpmux_rdpmux_schedule_emit_changed (skeleton, _mux_org_rdpmux_rdpmux_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
        g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
        g_object_notify_by_pspec (object, pspec);
    }
    g_mutex_unlock (&skeleton->priv->lock);
    g_object_thaw_notify (object);
}

static void
mux_org_rdpmux_rdpmux_skeleton_init (MuxOrgRDPMuxRDPMuxSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
    skeleton->priv = mux_org_rdpmux_rdpmux_skeleton_get_instance_private (skeleton);
#else
    skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, MUX_TYPE_ORG_RDPMUX_RDPMUX_SKELETON, MuxOrgRDPMuxRDPMuxSkeletonPrivate);
#endif

    g_mutex_init (&skeleton->priv->lock);
    skeleton->priv->context = g_main_context_ref_thread_default ();
    skeleton->priv->properties = g_new0 (GValue, 1);
    g_value_init (&skeleton->priv->properties[0], G_TYPE_VARIANT);
}

static GVariant *
mux_org_rdpmux_rdpmux_skeleton_get_supported_protocol_versions (MuxOrgRDPMuxRDPMux *object)
{
    MuxOrgRDPMuxRDPMuxSkeleton *skeleton = MUX_ORG_RDPMUX_RDPMUX_SKELETON (object);
    GVariant *value;
    g_mutex_lock (&skeleton->priv->lock);
    value = g_value_get_variant (&(skeleton->priv->properties[0]));
    g_mutex_unlock (&skeleton->priv->lock);
    return value;
}

static void
mux_org_rdpmux_rdpmux_skeleton_class_init (MuxOrgRDPMuxRDPMuxSkeletonClass *klass)
{
    GObjectClass *gobject_class;
    GDBusInterfaceSkeletonClass *skeleton_class;

    gobject_class = G_OBJECT_CLASS (klass);
    gobject_class->finalize = mux_org_rdpmux_rdpmux_skeleton_finalize;
    gobject_class->get_property = mux_org_rdpmux_rdpmux_skeleton_get_property;
    gobject_class->set_property = mux_org_rdpmux_rdpmux_skeleton_set_property;
    gobject_class->notify       = mux_org_rdpmux_rdpmux_skeleton_notify;


    mux_org_rdpmux_rdpmux_override_properties (gobject_class, 1);

    skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
    skeleton_class->get_info = mux_org_rdpmux_rdpmux_skeleton_dbus_interface_get_info;
    skeleton_class->get_properties = mux_org_rdpmux_rdpmux_skeleton_dbus_interface_get_properties;
    skeleton_class->flush = mux_org_rdpmux_rdpmux_skeleton_dbus_interface_flush;
    skeleton_class->get_vtable = mux_org_rdpmux_rdpmux_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
    g_type_class_add_private (klass, sizeof (MuxOrgRDPMuxRDPMuxSkeletonPrivate));
#endif
}

static void
mux_org_rdpmux_rdpmux_skeleton_iface_init (MuxOrgRDPMuxRDPMuxIface *iface)
{
    iface->get_supported_protocol_versions = mux_org_rdpmux_rdpmux_skeleton_get_supported_protocol_versions;
}

/**
 * mux_org_rdpmux_rdpmux_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-RDPMux-RDPMux.top_of_page">org.RDPMux.RDPMux</link>.
 *
 * Returns: (transfer full) (type MuxOrgRDPMuxRDPMuxSkeleton): The skeleton object.
 */
MuxOrgRDPMuxRDPMux *
mux_org_rdpmux_rdpmux_skeleton_new (void)
{
    return MUX_ORG_RDPMUX_RDPMUX (g_object_new (MUX_TYPE_ORG_RDPMUX_RDPMUX_SKELETON, NULL));
}
